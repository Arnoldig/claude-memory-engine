# История изменений

[English](CHANGELOG.en.md)

Здесь перечислены заметные изменения проекта. Формат по образцу [Keep a Changelog](https://keepachangelog.com/), нумерация версий по [семантическому версионированию](https://semver.org/).

## [Не выпущено]

### Добавлено
- **Снимок несохранённой работы перед командой, которая может её унести.** Страж git-команд перечисляет опасное, а потеря бывает не через git: `rm -rf`, `sed -i`, перенаправление в файл, скрипт. Перечнем такое не закрыть — обходы находятся быстрее, чем пишутся шаблоны (замерено: 11 обходов через оболочку из 24). Снимок меняет постановку: вместо «предотвратить потерю» — «сделать её восстановимой». Правки складываются в служебную ссылку git, рабочее дерево не трогается. Восстановить: `git show <ссылка>:<путь>` или `git stash apply <ссылка>`; перечень — `git for-each-ref refs/claude-snapshots`. Хранятся последние десять: перечень без предела становится нечитаемым, и найти в нём нужный снимок невозможно. Граница названа вслух — берутся только отслеживаемые изменения.

### Изменено
- **Страж git-команд перешёл на перечень РАЗРЕШЁННОГО.** Перечень запрещённого протекал трижды подряд: пропущенные написания флагов, затем пять глаголов, которых не было вовсе, затем одиннадцать обходов через оболочку из двадцати четырёх. Это свойство приёма, а не недосмотр: список опасного пополняется после каждой новой потери, то есть всегда задним числом. Перечень разрешённого конечен по построению — замер по журналам сессий дал 8431 вызов git и 85 глаголов, из которых двадцать покрывают 90% работы.

  Главный выигрыш — направление ошибки разбора: раньше «не разобрал» означало пропуск, отсюда и брались обходы через переменную; теперь означает блок. `checkout` различается вызовом самого git: ветка проходит, путь блокируется, двусмысленность считается потерей. Цена замерена на 600 реальных командах (1498 вызовов): заблокировано 22, это 1,5%, и все по делу.

### Исправлено
- **Страж git-команд не включался там, где терялось самое дорогое.** Он спрашивает git о несохранённом, а тот игнорируемые файлы не показывает — дерево считалось чистым. Команда очистки с соответствующим ключом уносила список приватных слов, который лежит вне git намеренно и не восстанавливается ничем; вместе с ним молча выключалась защита от утечки в публичный репозиторий.
- **Команды, снимающие защиту, проходили мимо.** Они сами ничего не теряют, поэтому проверка «есть что терять» их пропускала: запись `core.hooksPath` отключает проверку перед отправкой, алиас подменяет глагол, `core.pager` и `diff.external` выполняют произвольную команду — в том числе через безобидный на вид `git -c … log`. Теперь блокируются независимо от состояния дерева; чтение настроек не затронуто.

## [0.19.0] — 2026-07-20

### Изменено
- **Страж публикации перешёл на список РАЗРЕШЁННОГО** ([#18](https://github.com/Arnoldig/claude-memory-engine/issues/18)). Раньше он перечислял запрещённое: искал приватные слова в тексте команды. Замерено 2026-07-20 на живом страже: из 20 способов отправки текста наружу **11 проходили мимо** — содержимое собиралось ПОЗЖЕ момента проверки (подстановка команды и переменной, обратные кавычки, конвейер, перенаправление ввода, подстановка процесса). Пропуск в перечне запрещённого невидим и необратим: опубликованное не отзывается.

  Теперь разрешены три формы, где содержимое читается целиком ДО выполнения: буквальная строка в теле, файл с буквальным путём, поля api с буквальными значениями. Всё остальное в публикующей команде блокируется с объяснением, что переписать. Цена ошибки перевернулась: пропуск в перечне даёт видимую ложную тревогу вместо невидимой утечки.

  Признак «проверить нельзя» взят НЕ разбором shell: разбирать чужой синтаксис регуляркой уже пробовали, вышло семь способов обезоружить стража, правку откатили. Признак грубый и потому неуправляемый снаружи — наличие знака, способного породить или перенаправить текст. Обычной работы это не касается: страж вступает в дело только после того, как команда опознана как публикующая, поэтому конвейеры, подстановки и цепочки в обычных командах проходят свободно (закреплено отдельными тестами на пропуск).

  Обход для согласованной отправки — комментарий `# согласовано с владельцем` в самой команде: видимый в истории, в отличие от молчаливого пропуска.

### Исправлено
- **Страж git-команд был слеп к отложенной работе и шумел на её просмотре.** Он решает «есть что терять» по состоянию рабочего дерева, а отложенное в нём не видно: команда удаления отложенного при чистом дереве проходила молча, и работа терялась безвозвратно — «не увидели» читалось как «нечего терять». Теперь при таких командах проверяется и отложенное. Обратная сторона того же шаблона: показ списка и содержимого отложенного блокировался зря — замерено 48 пустых блокировок за неделю. Страж, мешающий смотреть, снимают целиком, вместе с настоящей защитой.
- **Проверка перед отправкой смотрела не на то, что уходит** — единственный слой, который утечку ПРЕДОТВРАЩАЕТ (всё серверное лишь обнаруживает её, когда коммиты уже на публичном сервере). Три дефекта, каждый замерен: выход по отсутствию файла пакета стоял ВЫШЕ защиты от утечки и гасил её целиком по причине, к утечкам отношения не имеющей; диапазон брался из upstream текущей ветки, а содержимое — из рабочего дерева, поэтому слово, живущее ТОЛЬКО в истории уходящих коммитов, не находилось вовсе; список слов, который не удалось прочитать, давал молчаливый пропуск. Теперь список ссылок читается со стандартного ввода (как его и передаёт git), проверяются патчи каждого уходящего коммита, а нечитаемый список блокирует отправку. Отсутствие списка по-прежнему пропуск: у стороннего участника этих слов нет, а хук, ломающий чужую отправку, снимут вместе со всей защитой.
- **Существовавший тест на эту проверку не мог упасть**: он сам создавал файл пакета и звал хук без входных данных, то есть был зелёным ровно на той настройке, где ни один из трёх дефектов не проявляется. Переписан.
- **Страж публикации не выполнял собственного обещания на нечитаемом файле.** В его шапке записано: после опознания публикации «не смогли проверить → блокируем», но неудачное чтение файла давало ПРОПУСК с доводом «файла нет — пусть решает `gh`». Довод неполон: неизвестно, чего нет — файла или прав на него, — а между проверкой и запуском файл может появиться. Через ту же ветку проходила подстановка процесса: пути `<(…)` не существует, проверка падала, страж пропускал, команда работала и публиковала.
- **Страж публикации падал на разобравшемся, но не словарном JSON.** Ввод `null` или `[]` давал трассировку вместо кода возврата, а для `PreToolUse` падение означает ПРОПУСК — отказ выглядел как разрешение. Разбор внутри стража падать не имеет права.
- **Относительный путь к файлу тела искался не от того каталога** — от каталога процесса стража, а не от каталога команды. Пока ненайденный файл молча пропускался, ошибка была невидимой; со строгой блокировкой она превратилась бы в ложную тревогу на законной команде, то есть в повод отключить стража.
- **Значение чужого рода в настройке больше не гасит движок молча** ([#21](https://github.com/Arnoldig/claude-memory-engine/issues/21)). Число вместо списка (`"topic_order": 42`) роняло саму загрузку конфига, а хуки fail-open превращали падение в выход нулём: движок выключался ЦЕЛИКОМ и без единого слова, и «выключен опиской» выглядело как «подсказывать нечего». Замер показал, что случаев вдвое больше, чем в заявке: часть значений проходила загрузку и роняла событие ниже неё, причём самодиагностика при этом рапортовала «всё в порядке».

  Лечение общее, а не по полю: род значения выводится из объявления самого датакласса, поэтому поле, добавленное завтра, попадает под защиту без правки проверки. Значение чужого рода отбрасывается, действует дефолт, а имя поля уходит в жалобу самодиагностики — не под `verbose`, иначе это снова молчание. Направление выбрано по цене ошибки: потерять одну настройку и сказать об этом дешевле, чем потерять всю память сессии и промолчать. Новое сообщение — `self_check.mistyped_key`.

- **Эталон форм закрытия получил третью координату — РЕГИСТР слова** ([#20](https://github.com/Arnoldig/claude-memory-engine/issues/20)). У стража отставания есть выход «промахнулись все зонды = законная полная замена». Пока зонды шли в одном регистре, копия дефолта, потерявшая нечувствительность к регистру, промахивалась на всех — и страж молчал о шаблоне, узнающем треть документированных GitHub форм. Ось разводит два случая: шаблон отстал или заменён целиком.

- **Ожидаемый ответ зонда больше не задан числом в двух местах.** Идентификатор стоял литералом и в шаблонах зондов, и в сверке рядом с порогом. Две копии одной величины расходятся молча: прогон показал, что при добавлении оси сверка начинает мерить не то — дефолт жалуется сам на себя и одновременно маскирует настоящую дыру. Теперь идентификатор объявлен один раз (`GITHUB_CLOSE_PROBE_ID`), ожидание берётся оттуда же, а число зондов нигде не записано числом — оно считается по осям.

- **Страж разрушительных git-команд знает все написания силового флага.** Пять команд уносили несохранённую работу молча: длинная форма у `clean` (сама по себе, с ключом каталогов и с точкой) и слипшиеся короткие флаги у `checkout`. Написания разошлись зеркально — одна команда знала слипшиеся, но не длинную форму, соседняя ровно наоборот, — потому что каждая строка списка перечисляла их сама. Теперь написания заданы одной константой. Найдено проверкой по последствию: команда выполняется в одноразовом репозитории, и опасной считается та, после которой несохранённого стало меньше; перечень форм при этом никем не переписывается вручную.

### Границы, названные вслух
- Описанная в [#20](https://github.com/Arnoldig/claude-memory-engine/issues/20) дыра со слагом после двоеточия — **не дефект, а решение с замером**: ветка, принимающая любой идентификатор после двоеточия, давала ложные закрытия на Conventional Commits (замерено на живой истории). Слаг — конвенция движка, а не форма GitHub, и требовать её от чужого шаблона библиотека не вправе.

## [0.18.0] — 2026-07-19

### Добавлено
- **Движок называет тему, которой нет в списке** ([#14](https://github.com/Arnoldig/claude-memory-engine/issues/14)). Урок с `topic: <слаг>`, которого нет в `topic_order`, попадал в раздел «⚠ No topic (add `topic:` to the frontmatter)» — то есть движок советовал добавить поле, которое в файле уже было, — а само увиденное значение не появлялось НИ В ОДНОМ канале: ни в оглавлении, ни в пульсе здоровья, ни в подробной диагностике. Опечатка в слаге была неотличима от отсутствия поля, и разбирать её приходилось перебором файлов вручную.

  Теперь это два разных, непересекающихся счётчика: «поля нет» и «значение не заведено». Первое чинится в УРОКЕ, второе — либо в уроке (опечатка), либо в КОНФИГЕ (тема настоящая, но её не завели); один заголовок на два случая уводил ровно в ту половину, которая неверна. Значение печатается во всех трёх каналах. Новые сообщения: `catalog.unknown_topic_note`, `health.unknown_topic`, `diag.unknown_topic_count`, `diag.unknown_topic_item`.

- **Пустая таксономия тем — жалоба, а не тишина** ([#14](https://github.com/Arnoldig/claude-memory-engine/issues/14)). `"topic_order": []` принималось молча и давало указатель без единого раздела: все уроки уезжали в ⚠-раздел независимо от своего поля `topic:`. Отличить это от «просто ещё нет уроков с темой» было нечем. Новое сообщение — `self_check.empty_topic_order`.

- **Отчёт самопроверки показывает покрытие перевода** ([#14](https://github.com/Arnoldig/claude-memory-engine/issues/14)). Замер на живом проекте: переопределено 8 ключей `messages` из 142, остальные 134 штатно деградируют на английский дефолт — по каждому ключу отдельно, поэтому соседние строки ОДНОГО чеклиста рендерились на разных языках. Ни ошибки, ни кода возврата. Отчёт `self_check.report()` (`python3 -m claude_memory.self_check`) теперь называет долю переведённого и примеры недостающих ключей. Новое сообщение — `self_check.report_messages_coverage`.

### Исправлено
- **`null` в списочном поле конфига ронял движок вместо возврата к дефолту.** `"topic_order": null` доезжал до конструктора как `None` и падал `TypeError`'ом в `topic_titles()` — далеко от причины: проверка `is not None` пропускала приведение к кортежам, но сам ключ из данных не убирала, поэтому дефолт датакласса не срабатывал. Зазор был общий для ВСЕХ полей `_TUPLE_FIELDS`, а не только для тем, и закрыт целиком: точечная починка означала бы, что следующее поле повторит ту же историю.
- **Описка в типе `messages` гасила весь вывод движка.** `"messages": []` (список, строка, число) доезжал до `msg()` как есть, а `.get` у списка нет — падало не одно сообщение, а всё, что печатает движок, включая самодиагностику, которая должна была об этой описке сказать. Функция, чья работа — никогда не падать, теперь проверяет тип. Найдено тестом на устойчивость, написанным по конвенции сюиты, а не самой заявкой.

### Границы, названные вслух
- **Отсутствие слага `core` в таксономии НЕ жалоба, и это осознанно.** Его читает ровно одно место — `bootstrap_topics_from_catalog`, разовый ПРЕВЬЮ-путь миграции на `topic:`. Жаловаться каждую сессию про путь, который выполняется один раз за жизнь проекта, — ложное срабатывание, а с него начинается привычка не читать жалобы вовсе. Закреплено тестом на ПРОПУСК.
- **Покрытие перевода — в отчёт самопроверки, а не в жалобы `SessionStart`.** Дописать сто с лишним ключей — работа не на пять минут, и жалоба каждую сессию превратилась бы в постоянный фон, который перестают читать (в `self_check` жалобы чинятся один раз навсегда — это его правило). Отчёт человек запрашивает сам: при настройке и после обновления движка. Граница закреплена тестом: перенос проверки в `warnings()` сломает его, то есть будет сделан осознанно, а не походя.
- **Движок по-прежнему НЕ правит чужой конфиг.** В заявке предлагалась команда, сливающая проектный конфиг с новым дефолтом. Сделана только читающая половина. Движок сегодня к конфигу либо не прикасается, либо удаляет его при `uninstall`; третий режим «правлю на месте» вносил бы новый класс молчаливых отказов — частично применённое слияние, потерянные `_*`-заметки, отчёт «что изменилось», который сам может недосчитаться, — ради задачи, которую человек решает руками по этому файлу.
- **Ключ-добавка `topic_extra` НЕ введён.** Предлагался как способ дополнить заводской список одной строкой вместо копирования целиком. Отвергнут по замеру: оба наблюдаемых конфига (живой проект и собственный пример репозитория `examples/claude-memory.config.ru.json`) заводские темы ВЫЧИТАЮТ, а не дополняют — пример убрал `docs`, проект убрал `testing` и `docs`. Слияние вернуло бы обоим ровно то, что убрано намеренно. Взамен появился бы второй источник истины и связка «оба ключа заданы», отказывающая молча при любой небрежности. Настоящий дефект этого узла — не «нельзя дополнить», а «замена не диагностируется», и закрыт он выше.
- **«Поле пустое» и «поля нет» по-прежнему неразличимы.** `parse_frontmatter` не кладёт пустые значения в результат, поэтому `topic:` без значения ниже по течению выглядит как отсутствие поля. Оба случая чинятся одинаково, так что разводить их незачем, но обещать различение нельзя.

## [0.17.0] — 2026-07-19

### Добавлено
- **Страж размера файла инструкций проекта (`CLAUDE.md`)** ([#16](https://github.com/Arnoldig/claude-memory-engine/issues/16)). Движок следил за размером горячего ядра памяти и за размером отдельных уроков, но за файлом ПРАВИЛ — не следил ничто, хотя именно он читается в каждой сессии целиком. Замер на живом проекте: 172 строки при 59 000 знаков, то есть вчетверо больше всего бюджета горячего ядра, и ни одного предупреждения за всё время роста.

  **Почему счёт строк тут не работает.** В стиле письма «абзац — одна строка, без жёстких переносов» одна строка бывает длиной в тысячи знаков, поэтому по числу строк раздутый файл выглядит нормальным. Страж меряет ЗНАКИ. Байты тоже отвергнуты: на кириллице их вдвое больше, и один и тот же порог значил бы разное в зависимости от языка проекта.

  Новые настройки: `instructions_budget_chars` (ориентир, по умолчанию 20 000 знаков; `0` выключает) и `instructions_files` (какие файлы стеречь, по умолчанию `["CLAUDE.md"]`). Новое сообщение — `bloat.instructions_large`.

### Границы, названные вслух
- **Это ОРИЕНТИР, а не порог, и текст стража обязан это удерживать.** Технического лимита у файла инструкций нет: документация Claude Code говорит дословно, что он загружается целиком независимо от длины, обрезки не происходит. 20 000 — пересчёт рекомендации Anthropic «до 200 строк» под длинную строку, то есть ОЦЕНКА. Отсюда три решения, каждое против одного и того же прочтения «раз есть число, надо резать до числа»: одна точка срабатывания вместо пары «предупреждение/превышение» (пара порогов вокруг оценки — псевдоточность); только предупреждение, никогда не блокировка; и текст, который называет настоящую цену — часть правил начинает МОЛЧА не исполняться — и прямо говорит не сокращать до числа. Живой прецедент: файл сжали с 59 000 до 28 600 знаков, а попытку дожать до 20 000 отвергли — слепой экзамен показал, что вырезанные ради цифры строки стоили дороже выигрыша от размера.
- **Жёсткий порог с бесшумной потерей содержимого существует, но у ДРУГОГО файла** — у авто-памяти: Claude Code грузит только первые 25 КБ либо первые 200 строк, что наступит раньше. Оба механизма названы в тексте стража порознь, иначе жёсткость одного молча переносится на мягкость другого. Существующие сообщения про горячее ядро при этом НЕ тронуты намеренно: у потребителей они переопределены своими переводами, правка английского дефолта до них не дойдёт, и получилось бы ровно то отставание копии, которое движок стережёт.
- **Замер идёт по тому, что реально доходит до модели.** Claude Code вырезает блочные HTML-комментарии из файла инструкций до подачи в контекст (комментарии внутри блоков кода при этом сохраняются). Меряя сырой файл, страж мерил бы не тот объект и наказывал бы за правильное поведение: пояснения для сопровождающих, честно вынесенные в комментарий, весят тысячи знаков, которых модель не видит никогда.
- **Два канала, потому что одного мало.** Хук правки видит только `Write|Edit|MultiEdit`; правку через `sed`, внешний редактор или слияние ветки он не видит вовсе, и его молчание неотличимо от «файл в порядке». Поэтому тот же замер повторяется на `SessionStart` — сторожим РЕЗУЛЬТАТ, а не канал. На правке нудж РАЗОВЫЙ на (сессию, файл): без этого сессия, сокращающая раздутый файл десятком правок, получала бы одно и то же сообщение на каждую (замерено: три правки — три текста по 816 знаков), то есть страж заливал бы контекст ровно тогда, когда его освобождают. На старте троттлинг по ИЗМЕНЕНИЮ размера, а не по времени: пока файл не трогали, движок молчит сколько угодно сессий, но после починки и повторного роста до того же числа говорит снова — маркер переписывается всегда, иначе запись о старом размере пережила бы починку и страж ослеп бы молча.
- **Пути разрешаются от корня проекта, от рабочего каталога сессии и от ВСЕХ его предков** — так же, как Claude Code собирает файлы инструкций, идя по дереву вверх. Обхода ВНИЗ нет: вложенные файлы инструкций Claude Code при старте не грузит, их размер стоит иначе.
- **Правка файла инструкций мимо инструментов и вне сессии** (другая машина, чужой коммит) до `SessionStart` не видна — это цена того, что движок не следит за файлом непрерывно.

### Разработка (в пакет не входит)
- **Добавлен линтер (`ruff`) — в CI, перед тестами.** Набор правил намеренно узкий: `E9` (синтаксис и ошибки времени исполнения) и `F` (необъявленное имя, мёртвый импорт, переопределение). Это ровно домашний класс дефектов движка — код запускается, сюита зелёная, а ветка не работает и молчит об этом; тесты такое видят не всегда, потому что редкая ветка может не исполняться ни разу. Стилевые правила (длина строки, порядок импортов, кавычки) НЕ включены осознанно: линтер, спорящий о вкусах, начинают глушить `noqa`, а вместе с вкусами глохнут настоящие находки — тот же исход, что у стража, выключенного, чтобы не мешал. Первый прогон на живом коде дал девять находок (четыре мёртвых импорта в пакете, четыре в тестах, одно затенение импорта) и ноль шума; все исправлены. `target-version` привязан к самой старой заявленной версии Python, иначе линтер пропустил бы синтаксис, который у пользователя не соберётся. Оба свойства — запуск линтера в CI и узость набора — закреплены тестами (`test_ci_actually_runs_the_linter`, `test_linter_rule_set_stays_narrow_and_targets_the_oldest_python`) и проверены мутацией: связка «числится в зависимостях, но не гоняется» создаёт полную видимость проверки, которой нет.
- **Ревью нашло восемь дефектов ВНУТРИ самой починки, и два из них воспроизводили тот самый класс, который эта заявка и закрывает.**
  1. **Незакрытый комментарий обнулял замер.** Строка `<!--` без закрытия глотала файл до конца: замер файла в 10 000 знаков давал НОЛЬ, то есть страж замолкал навсегда ровно там, где обязан кричать. Разбор, молча отдающий пустоту на непонятном вводе, — общий знаменатель всех прецедентов движка, воспроизведённый внутри стража, который от этого и заведён. Теперь незакрытый комментарий считается опечаткой, а не комментарием, и текст возвращается в замер.
  2. **Смесь маркеров блока кода занижала замер.** Любая строка с ``` или `~~~` переключала флаг «внутри кода», не помня, чем блок открыт, поэтому содержимое блока начинало резаться как комментарий: файл в 34 знака мерился как 12. Занижение опаснее завышения — страж молчит там, где обязан говорить.
  3. **Сессия, открытая в ПОДКАТАЛОГЕ рабочей копии, теряла оба канала разом.** Корень из конфига указывает на главную копию, `cwd` на подкаталог, файл не совпадал ни с одним. Закрыто обходом дерева вверх.
  4. **Файл не в UTF-8 ронял хук.** `read_text` бросает `UnicodeDecodeError` — это `ValueError`, а не `OSError`, и он вылетал наружу, унося с собой уже посчитанный результат СОСЕДНЕЙ проверки памяти в том же событии, а на старте — предупреждения обо всех остальных файлах. Дословный повтор прецедента 0.16.0, где один байт чужой кодировки в списке приватных слов отключал стража целиком.
  5. **На файловой системе, нечувствительной к регистру, два канала расходились в показаниях**: хук правки на файле `Claude.md` молчал (строки не равны), а замер на старте срабатывал и печатал имя `CLAUDE.md`, которого на диске нет.
  6. Сообщение печатало голый `CLAUDE.md`, поэтому в worktree-сессии два разных файла давали две неразличимые строки — человек не мог понять, какой чинить.
  7. Нудж на правке повторялся на каждую правку (см. выше).
  8. **Самое дорогое: НИ ОДНА из двух точек подключения стража не была закреплена тестом.** Замерено: обе выдернуты — вся сюита зелёная. Страж мог быть мёртв в проде, а набор тестов уверял в обратном. Добавлены два смоук-теста через настоящую bash-обёртку хука.
- Каждое свойство закреплено ОТДЕЛЬНЫМ тестом и проверено мутацией — десять мутаций роняют свой тест. Первая редакция теста «молчим на чужих файлах» мутацию ПЕРЕЖИЛА: стерегомого файла в ней не существовало, поэтому молчание объяснялось не сверкой пути, а тем, что читать было нечего. Тест исправлен — ровно тот класс, что «тест, который не может упасть, хуже отсутствующего».

## [0.16.0] — 2026-07-19

### Исправлено
- **Привратник закрытия видел не более ОДНОГО закрытия на коммит; остальные терялись молча** ([#15](https://github.com/Arnoldig/claude-memory-engine/issues/15)). `extract_closed_task` построена на `re.search` — побеждает ПЕРВОЕ совпадение. Значит гейт узнавал максимум одно закрытие, а потерянное закрытие — это урок, которого не потребовали: ровно то, ради чего гейт и заведён. Молчание при этом неотличимо от «закрытий не было» — общий знаменатель всех прецедентов движка.

  **Отдельный сценарий, самый неприятный: РАССКАЗ о форме закрытия затеняет НАСТОЯЩЕЕ закрытие ниже.** Коммит, правящий шаблон закрытия и закрывающий задачу об этом, содержит в теле фразу вида «шаблон расширен формами `Closes: #123`» — это проза, не закрытие. Извлекался фантом `123`, а настоящая задача из футера терялась. То есть ложное срабатывание порождало ложный ПРОПУСК: урок требовали для несуществующей задачи, а для реально закрытой — нет. Порядок, при котором это происходит, самый естественный: конвенция обоих проектов-потребителей кладёт закрывающую фразу в КОНЕЦ сообщения, а рассказ о правке — в тело выше.

  Починка: новая `extract_closed_tasks` (множественное число) РЯДОМ со старой — `finditer`, первая непустая группа на каждое совпадение, дедупликация с сохранением порядка, потолок числа id. `closure_reminder` перешла на неё и блокирует, если урока нет хотя бы по одному id; все недостающие перечисляются в существующем ключе `stop_check.closure_reminder`, поэтому переопределения текста у потребителей продолжают работать без правки.

  **Замер на живых данных** (1066 коммитов трёх репозиториев): у одного потребителя терялось три закрытия, из них ДВА — настоящие задачи. Контракт надмножества (первый элемент результата = прежний результат) соблюдён на всей истории и закреплён тестом, поэтому ничего ловившегося прежде потеряться не может.

### Границы, названные вслух
- **Старая `extract_closed_task` осталась и НЕ сменила тип возврата** — это не осторожность, а обязательное условие. На её возврате-СТРОКЕ держится страж отставания шаблона (`self_check.close_pattern_lag_issues` сравнивает результат с `"42"`): список не равен строке никогда, все 18 зондов стали бы «не узнаны», сработала бы ветка «ноль форм = законная полная замена», и страж молча умер бы. Второе: `task_lesson_recorded` подставляет результат в `re.escape`, где список даёт `TypeError`, а широкий `except` диспетчера погасил бы вместе с ним ВЕСЬ Stop-гейт — на каждом закрывающем коммите.
- **Отличать цитату от действия намеренно НЕ пытаемся.** Тела коммитов у обоих потребителей — русская проза с примерами форм; любой «скелет» съел бы настоящие закрытия. Прецедент соседнего проекта: построили разбор кавычек, самотест дал 114/114, ручная проверка нашла ЧЕТЫРЕ живые дыры, правку откатили целиком. Цена принятого решения: проза о форме теперь даёт лишний id НАРЯДУ с настоящим закрытием, а не ВМЕСТО него — это строго лучше прежнего. Замер: два лишних id на 1066 коммитов, гасятся упоминанием в уроке.
- **Шаблон не тронут.** Дефект был у ПОТРЕБИТЕЛЯ результата, `GITHUB_CLOSE_KEYWORDS`/`GITHUB_CLOSE_SYNTAXES` верны.
- **При нескольких id текст блокировки слегка теряет согласование по числу** («коммит закрывает задачу #58, #tema-zaslona… урока про неё нет»). Принято намеренно: отдельный ключ для множественного числа заставил бы каждого потребителя обновить своё переопределение, а необновлённое переопределение — это молчаливое отставание, то есть ровно тот класс дефекта, который движок и стережёт. Смысл текста при этом не страдает: все недостающие номера названы, а именно они человеку и нужны.

### Разработка (в пакет не входит)
- **Четыре способа молча обойти стража приватных слов** ([#15](https://github.com/Arnoldig/claude-memory-engine/issues/15)). Общий контекст, ЗАМЕРЕННЫЙ прогоном, а не выведенный из документации: превышение таймаута хука PreToolUse **пропускает** вызов, а не блокирует его. Контрольный опыт (хук, отвечающий кодом 2) вызов заблокировал; хук, спящий дольше таймаута, — вызов выполнился, побочный эффект состоялся. Документация Claude Code об этом исходе не говорит вовсе. Следствие: любой способ ЗАМЕДЛИТЬ стража есть способ его ОБОЙТИ, причём молча — в журнале не остаётся ни блокировки, ни ошибки. Цена разбора становится вопросом корректности, а не скорости.

  1. **Квадратичный откат в образце адреса почты.** Два открытых жадных повтора вокруг `@` на входе БЕЗ `@`: 5 КБ — 0,13 с, 20 КБ — 2,10 с, 40 КБ — 8,66 с (вчетверо на каждое удвоение). Подушка набирается без всякого злого умысла — список идентификаторов через запятую, JSON в одну строку, длинное основание закрытия. Перенос строки откат рвёт, поэтому многострочный текст проходил быстро, а тот же объём одной строкой — нет: страж отказывал ВЫБОРОЧНО и незаметно. Повторы ограничены сверху по смыслу поля (локальная часть ≤ 64 по RFC 5321, домен ≤ 255), адрес длиннее невалиден. Стало 0,08 с на 40 КБ.
  2. **Файл тела читался без проверки типа и предела размера.** Путь берётся из чужой команды; на именованном канале хук не завершился за 12 секунд. Теперь тип проверяется `os.stat` ДО открытия: не начинать чтение правильнее, чем прерывать его таймером, — путь полностью подконтролен тому, чью команду проверяют.
  3. **Один байт не в UTF-8 в списке слов отключал стража ЦЕЛИКОМ.** Список правят руками, и одна вставка из cp1251 роняла процесс `UnicodeDecodeError` ещё ДО образцов ключей — переставало работать и то, что от списка вообще не зависит. Замер: код 1 и traceback вместо кода 2, токен уходил незаблокированным. Код 1 блокировкой не является, поэтому со стороны это выглядело как обычная работа: защиты нет, но и внятной жалобы тоже. Тот же дефект был во втором чтении (файл исключений) — асимметрию легко пропустить глазами, потому что соседняя строка уже читала с `errors="replace"`.
  4. **Короткие формы флага не досматривались.** `-F`, `--input`, `--file` давали код 0, хотя `--input` уже числился каналом публикации в самом же перечне `PUBLISH`: канал признан публикующим, а содержимое его не читалось. Разобрана и форма `gh api -F key=@file`, где путь лежит после `=@`.

- **Единственное отступление от fail-open, названное вслух в шапке хука.** По ошибкам РАЗБОРА страж по-прежнему молчит и пропускает: чужой JSON, не тот инструмент, отсутствующий список слов — во всех этих случаях он либо не понял вход, либо ему нечего защищать. Но ПОСЛЕ того как команда опознана как публикация, действует обратное правило: не смогли проверить → БЛОКИРУЕМ. Три таких исхода: сторожевой таймер (SIGALRM, дедлайн строго меньше таймаута хука — инвариант закреплён тестом), файл тела не обычного типа, файл тела сверх предела. Обоснование одно: не уложились или не прочли — значит НЕ ЗНАЕМ, есть ли запретное, а необратимая публикация дороже одного лишнего разбора. Смысл таймера в том, чтобы исход был выбором СТРАЖА, а не среды.
- **Проверки стража теперь МЕРЯЮТ ВРЕМЯ**, а не только исход, и на входах в десятки килобайт: прежний набор все четыре дефекта пропускал, потому что на коротком входе поведение было правильным. К каждому новому блокирующему исходу добавлен парный тест на ПРОПУСК — набор из одних блокирующих случаев зеленеет и на полностью сломанном страже, поскольку bash отдаёт код 2 и при синтаксической ошибке. Все 11 новых проверок прогнаны против старого хука и падают на нём.
- **Три дефекта нашло ревю ВНУТРИ самой починки** — записано, потому что каждый из них воспроизводил класс, который эта же правка и закрывает.
  1. **Разбор числа из переменной окружения ронял процесс.** `int("abc")` бросает `ValueError`, непойманное исключение — это код 1, а код 1 НЕ БЛОКИРУЕТ. То есть опечатка в ручке молча снимала стража целиком: дословно тот дефект, что чинился третьим пунктом (не-UTF8 в списке слов), воспроизведённый внутри его же починки. Непонятное значение теперь возвращает умолчание; верхняя граница добавлена, потому что `signal.alarm` на огромном числе бросает `OverflowError` — снова код 1.
  2. **Обход одной буквой закрыли, обход одним пробелом оставили.** Первая правка требовала разделитель между флагом и значением и пропускала прилипшую краткую запись `-Fфайл`, `-Fключ=@файл`, а также `--field` — документированную длинную форму `-F` у `gh api`. Замер: три валидных вызова `gh` из четырёх публиковали содержимое файла непроверенным.
  3. **Правка сделала стража СЛАБЕЕ, чем он был.** Разбор формы `ключ=@файл` применялся ко ВСЕМ флагам, поэтому обычный путь со знаком равенства (`dir=ty.md`) переставал читаться — то, что прежняя версия проверяла, новая пропускала. Разбор ограничен теми флагами, где такая форма существует.

  Повторное ревю тех же правок нашло ЕЩЁ четыре, того же класса — что само по себе вывод: одного прохода мало.
  4. **Дыра ПЕРЕЕЗЖАЛА, а не закрывалась.** Первая правка ловила `-F=файл`, но не `-Fфайл`; вторая — ровно наоборот. `pflag` съедает один знак равенства после краткого флага, и `gh` принимает обе записи. Закрыто одним `=?`, обе формы под тестом вместе — именно потому, что порознь они уже разъезжались.
  5. **Две одинаково вероятные опечатки давали ПРОТИВОПОЛОЖНЫЙ исход.** `abc` в дедлайне откатывался к умолчанию и защищал, а `-5` молча снимал таймер. Теперь выключает только ТОЧНОЕ «0» — намерение обязано отличаться от описки написанием.
  6. **Тест давал ложную уверенность.** Проверка «мусор в ручке не роняет стража» ловила приватное слово СПИСКОМ СЛОВ, то есть ветку таймера не исполняла вовсе и осталась бы зелёной при полностью выключенном таймере. Добавлена парная проверка на ЧИСТОМ тексте, где сработать может только таймер.
  7. **Оставался один выход с кодом не 2 после опознания публикации:** путь с нулевым байтом даёт у `os.stat` не `OSError`, а `ValueError`. Труднодостижимо, но инвариант шапки не должен иметь лазеек — их находят не по одной.

  Все семь закреплены тестами и подтверждены замером против прежних версий. Вывод для набора: тестов на новое поведение недостаточно — нужен и замер против того, что было, иначе регресс ВНУТРИ починки выглядит как успех. И отдельно: зелёный тест стоит ровно столько, сколько стоит доказательство, что он краснеет на сломанном коде.
- +43 теста (642 против 599).
- **Два замка против утечки внутренних названий в публичный репозиторий.** Поводом стал разбор: названия двух рабочих проектов попали в комментарии заявок #6 и #13 и в комментарии кода. Комментарии заявок git не видит вовсе — их не поймал бы ни один git-хук, а отозвать отправленный текст нельзя: правка остаётся в истории правок GitHub.

  `.claude/hooks/private_words_guard.sh` (PreToolUse на Bash) блокирует ПУБЛИКУЮЩИЕ команды `gh` — создание и правку заявок, комментариев, PR, релизов, gist, а также `gh api` методами записи; читает и текст из `--body-file`/`--notes-file`, иначе проверял бы имя файла вместо содержимого. Читающие команды пропускает молча: страж, ломающий `grep` по этим же словам, сделал бы невозможным сам аудит и был бы снят в первый же день. Второй замок — блок в `.githooks/pre-push`: он видит то, чего не видит первый, — содержимое файлов и сообщения уходящих коммитов.

  **Список слов не версионируется намеренно** (`.claude/private-words.txt` в `.gitignore`): файл, перечисляющий то, что нужно скрыть, раскрывает это сам. Нет списка → оба замка молчат, поэтому внешнему участнику проекта они не мешают. По той же причине тесты работают на выдуманных словах и подкладывают собственный список — иначе стали бы ещё одной копией скрываемого.

  26 тестов, зеркальная конвенция соседних стражей (блок только с текстом, пропуск только при пустом stderr). Проверено мутацией: «всегда молчащий» вариант валит 10 тестов из 26. Отдельный тест следит, что сам список остаётся вне git — проверяется результат `git check-ignore`, а не наличие строки в `.gitignore`, потому что правило могло быть перекрыто другим шаблоном.
- **Шаблоны поверх списка слов: ключи, почта, телефон.** Список бессилен там, где секрет каждый раз новый, — узнаётся формат. Шаблоны живут в КОДЕ, а не в приватном файле: формат токена публичен (его печатает сам провайдер), скрывать нечего, а версионировать полезно — правка проходит ревью и тест. Работают и при отсутствии приватного списка: ключ не должен уехать в публичный комментарий и у того, кто списка не заводил (дефект нашёл тест — до правки шаблоны без списка не выполнялись вовсе).

  Секрет в тексте ошибки печатается ОБРЕЗАННЫМ: полная копия в журнале сессии — это ещё одна утечка. Телефон ловится только в международной записи со знаком «плюс»: шаблон пошире принимал бы версии, даты и хеши, а ложно блокирующего стража снимают целиком вместе с пользой. Служебные адреса (`noreply`, `example.com`) пропускаются — их не прячут, а вписывают намеренно.

  **Главный слой против ключей — не этот.** В репозитории включены секретное сканирование GitHub и защита при пуше: они знают форматы сотен провайдеров и обновляются сами. Локальный замок — упреждающий: ловит ДО отправки, когда текст ещё можно молча поправить, не оставляя следа.
- Из кода, тестов и хука убраны названия рабочих проектов: происхождение решений описано обезличенно, тестовые пути заменены на нейтральные с сохранением сути проверок (кириллица, подчёркивание, длина слага). Историю коммитов решено не переписывать — цена (все хеши, 16 тегов, битые ссылки в разборах заявок) выше пользы.

## [0.15.0] — 2026-07-18

### Исправлено
- **Дефолт научился второму документированному написанию формы закрытия** ([#13](https://github.com/Arnoldig/claude-memory-engine/issues/13)). GitHub принимает и `Closes #42`, и `Closes: #42` — двоеточие документировано дословно («The keywords can be followed by colons or in uppercase»). Движок вторую форму не узнавал: задача на GitHub закрывалась, привратник урока молчал. Тот же класс, что семья `resolve` в 0.10.0, только по другой координате — не СЛОВО, а НАПИСАНИЕ. И здесь отстал не потребитель, а САМ дефолт — от документации.

  **Почему после двоеточия принимается только ЧИСЛО, а после пробела — число или слаг.** Это не педантизм, а измеренная защита. Слаг (`#memory-lib-cutover`) — конвенция самого движка: GitHub на нечисловой id не реагирует вовсе. А `слово: #слаг` в начале темы коммита — это Conventional Commits (`fix: #тема — что сделано`), и таких тем у живого проекта-потребителя 619. Замер на его истории в 2258 коммитов: ветка, принимающая после двоеточия любой id, даёт **8 ложных закрытий**, ветка «только число» — **0**. Цена ложного не косметическая: Stop блокируется, пока не найдётся урок про несуществующую задачу. Числовая семантика после двоеточия ровно зеркалит GitHub — ветка существует потому, что документирована им, и наследует его правила.

### Изменено
- **Эталон получил вторую координату** — `stop_check.GITHUB_CLOSE_SYNTAXES`. Проверка `close_pattern_lag_issues` теперь строит зонды декартовым произведением «слово × написание» (18 зондов вместо 9), то есть замечает и отставание по написанию, а не только по слову.

### Ловушка, найденная разведкой ДО того, как сработала
- **Наивное расширение эталона ИНВЕРТИРОВАЛО бы стража.** У проверки есть выход «узнано ноль форм = законная полная замена под чужой трекер». Порог сверялся с числом СЛОВ (9). При двух написаниях отставшая копия даёт ровно 9 промахов из 18 — число совпадает, и страж принял бы отставшую копию за полную замену и ЗАМОЛЧАЛ, а настоящую полную замену (18 из 18) наоборот обвинил. Воспроизведено прогоном до правки: с наивным расширением оба живых конфига молчали, а `DONE-(\d+)` жаловался. То есть проверка, заведённая в 0.14.0 против молчаливого отставания, начала бы работать наоборот — ровно тем способом, против которого создана. Порог теперь выводится из ДЛИНЫ СПИСКА ЗОНДОВ: замороженное число или множитель «×2» вернут ту же ловушку при третьем написании. Закреплено тестом `test_lag_caught_when_only_spelling_is_behind`.

### Границы, названные вслух
- **Слитная форма `Closes:#42` принимается, хотя НЕ документирована** (в документации все примеры с пробелом после двоеточия). Принято намеренно шире документации: намерение автора недвусмысленно, а цена ошибки асимметрична — лишний вопрос про урок виден, объясним и гаснет по сроку годности метки, тогда как пропущенное закрытие невидимо. В ЭТАЛОН слитная форма НЕ внесена: требовать от чужого шаблона больше, чем документирует GitHub, библиотека не вправе. Эталон строго уже дефолта, и это направление проверяется тестом.
- **Форма с чужим репозиторием `Fixes octo-org/repo#100` не покрыта осознанно.** Она закрывает ЧУЖУЮ задачу, а `task_lesson_recorded` ищет `#100` в СВОЕЙ памяти — совпадение с сотой задачей своего проекта дало бы либо ложный пропуск гейта, либо требование урока по неверному ключу. Нынешнее молчание на этой форме — корректное поведение, а не пробел.
- **`GH-42` не трогаем:** это документированная форма ССЫЛКИ, но не форма закрытия — проверено по документации.
- Внешний проект с Conventional Commits и ЧИСЛОВЫМИ ссылками (`fix: #123 …`) получит новые срабатывания. Они ИСТИННЫЕ: по документированному примеру `Fixes: #10` GitHub на таком коммите сам закроет задачу. Сказано здесь, чтобы будущий аудит ложных срабатываний не открыл заявку заново.
- +15 тестов (535 против 520).

### Порядок выпуска
- Шаблоны обоих проектов-потребителей пересобраны поверх нового дефолта ДО обновления движка у них — так раскатка проходит без единого дня жалоб. Это безопасно: мигрированный шаблон является надмножеством прежнего, и проверка версии 0.14.0 на нём молчит (проверено на их живых конфигах до обновления).

## [0.14.0] — 2026-07-18

### Добавлено
- **Самодиагностика говорит вслух, когда проектный шаблон закрытия ОТСТАЛ от дефолта** ([#8](https://github.com/Arnoldig/claude-memory-engine/issues/8)). Новая жалоба `self_check.close_pattern_lag`: `task_close_pattern` не узнаёт часть из девяти официальных слов-закрытий GitHub.

  **Какой класс дефекта это закрывает.** Часть полей потребитель копирует из дефолта, чтобы ДОПИСАТЬ своё, а не заменить: `task_close_pattern` берут целиком и добавляют русскую ветку. С этой минуты копия заморожена на том виде, какой дефолт имел в день копирования, а библиотека свой дефолт расширяет. Семья `resolve/resolves/resolved`, добавленная в 0.10.0, не доехала НИ ДО ОДНОГО потребителя: полтора релиза `Resolves #N` тихо не распознавался в обоих боевых проектах. Отставший шаблон коварнее узкого — он был ПРАВИЛЬНЫМ, его никто не писал криво, и повод перепроверить не возникает. Соседние классы уже были закрыты: СЛОМАННЫЙ ловит `bad_regex_issues` с 0.9.7, а ОТСТАВШИЙ компилируется, работает и молчит.

  **Признак — «частичное покрытие», а не «отличается от дефолта».** Сравнивать текст с дефолтом нельзя дважды. Во-первых, переопределение НАМЕРЕННО: жалоба «у тебя не как в дефолте» навязчива, неустранима, и её отключат первой — это ровно ошибка, разобранная в [#5](https://github.com/Arnoldig/claude-memory-engine/issues/5). Во-вторых, текстовый признак ломается ровно на целевом случае: отставшая копия содержит СТАРЫЙ дефолт как подстроку, то есть выглядит «построенной на дефолте» и после того, как отстала. Поэтому судим по ПОВЕДЕНИЮ — гоняем девять форм через тот же `extract_closed_task`, который вызывает боевой страж.

  **Три случая молчания, и все законные:** ноль узнанных форм (полная замена под чужой трекер), все девять (покрытие есть, что бы шаблон ни добавлял сверху), выключенный `task_close_lesson_gate` (жалоба на неиспользуемый шаблон была бы шумом). Ручки «не жалуйся на неполноту» намеренно НЕТ: GitHub закроет задачу на любом из девяти слов независимо от мнения движка, значит частичный шаблон недосрабатывает объективно, а такая ручка воспроизвела бы исходный класс — страж, выключенный, чтобы не мешал.

  **Проверено на живых данных, а не только тестами:** на текущих конфигах обоих боевых проектов проверка молчит, на их же снимках до правки — сообщает ровно про семью `resolve`. То есть она поймала бы дефект, реально проживший полтора релиза.

### Изменено
- **Эталон девяти слов-закрытий сведён в ОДНО место** — `stop_check.GITHUB_CLOSE_KEYWORDS`. Он жил тремя несвязанными копиями: кортеж в `tests/test_examples_sync.py`, кортеж в `tests/test_repo_invariants.py` и проза в комментарии `config.py`. Добавить четвёртую копию ради проверки на отставание значило бы воспроизвести дефект внутри его же починки. Обе тестовые копии заменены импортом, комментарий конфига стал указателем на константу.
- Новый замок: дефолтный `task_close_pattern` обязан узнавать КАЖДУЮ форму константы — теперь дефолт и эталон не могут разъехаться молча.

### Границы, названные вслух
- **Поле, к которому применим признак «отстало», ровно одно.** Проверены все поля конфига по трём категориям: эталон (дефолт зеркалит закрытый внешний перечень), заготовка (проект законно заменяет целиком), переключатель. Эталон — только `task_close_pattern`. Ближайшие кандидаты и почему выпали: `lesson_prefixes` — с 0.10.0 не решает «что такое урок» (на это отвечает `lesson_files.is_lesson_file`), то есть перестал быть гейтом; `session_close_pattern` — его дефолт нейтральная английская фраза-заготовка, проект ОБЯЗАН задать свои формы; `known_model_substrs` / `strongest_model_substr` — самокорректируются рантайм-стражами. Поэтому механизм намеренно узкий: реестр «поле → эталон» с единственной записью был бы обобщением вперёд спроса, хотя форма возврата общая и второе поле добавится строкой.
- +12 тестов (520 против 508).

### Разработка (в пакет не входит)
- **Проверка основания у заявок, закрытых АВТОМАТИЧЕСКИ** — `.claude/hooks/issue_close_basis_audit.sh` (Stop). Найдено на собственной заявке #6: её закрыло вливание PR (`Resolves #6` стояло и в теле squash-коммита, и в описании), команда `gh issue close` не выполнялась вовсе, и `issue_close_basis_guard.sh` не вызывался — он стережёт только команду. Основание дописывали руками через пять минут.

  **Почему проверяется РЕЗУЛЬТАТ, а не канал.** Заявку закрывают шестью путями (ключевое слово в коммите, ключевое слово в описании PR, веб, мобильное приложение, REST/GraphQL, бот), и две точки не сторожатся принципиально: `gh pr create` без флагов (текст пишется в редакторе, его ещё нет) и `gh pr merge --squash` без `--subject/--body` (сообщение сочиняет сервер) — проверено экспериментом с подменой `gh` на регистратор аргументов. Сторожить каждый канал — гонка, которую не выиграть; это ровно урок заявки #6, где страж знал единственный источник сигнала.

  **Признак — `ClosedEvent.closer` из GraphQL, и только он.** Временное окно «комментарий рядом с закрытием» проверено и отвергнуто на живых данных: у #6 основание отстало на 294 с (окно 300 с сработало бы с запасом в шесть секунд, окно 60–120 с дало бы ложную тревогу), а за 69 минут ДО закрытия там лежит содержательный комментарий на 3741 знак, не являющийся основанием, — окно назад засчитало бы его и промолчало. В REST признака нет вовсе: `commit_id` у события `closed` равен null и при ручном, и при автоматическом закрытии.

  17 тестов на фикстурах (`gh` подменяется заглушкой, сеть не нужна). Проверены мутациями: мёртвый хук валит 16 тестов из 17, «всегда молчащий» — 9 из 17.
- **Запрет фраз закрытия в коммитах здесь ОТКЛОНЁН осознанно**, хотя именно это правило действует у обоих проектов-потребителей (и соблюдается там идеально: 0 нарушений на 436 коммитов за месяц). Причина: эта библиотека — про распознавание фраз закрытия, и фраза здесь рабочие ДАННЫЕ (тесты, примеры, changelog, докстринги). Замер по истории: оба найденных совпадения `clos|fix|resolv + #` в сообщениях коммитов оказались ложными — это коммиты вида `fix(stop_check): распознавать закрытие… не только англ. Closes #N`. Страж по фразе давал бы здесь систематические ложные срабатывания, а ложно блокирующего стража сносят целиком вместе с пользой.
- Правило «основание пишется в заявку ДО вливания закрывающего запроса» — в `CONTRIBUTING.md`. Детектор без предписанного правильного поведения превращает каждое срабатывание в тушение пожара вместо проверки дисциплины.
- Границы обоих стражей названы вслух в их шапках, включая непокрытое: закрытие кнопкой в вебе без комментария не отличимо от дисциплинированного `gh issue close --comment` (оба дают `closer=null`) и не ловится ничем.

## [0.13.0] — 2026-07-18

### Исправлено
- **Страж закрытия задачи перестал слепнуть на закрытии через GitHub Issues** ([#6](https://github.com/Arnoldig/claude-memory-engine/issues/6)). `task_close_lesson_gate` узнавал о закрытии ЕДИНСТВЕННЫМ способом — читал текст последнего коммита (`git log -1 --format=%B`). Пока задачи вели в markdown-трекере, закрытие И БЫЛО коммитом, правившим файл трекера, и это совпадало. После перехода на GitHub Issues задачу закрывают командой `gh issue close`, которая коммита не создаёт вообще: страж честно не находил совпадения и молчал.

  **Насколько велика была дыра.** Замер на двух живых проектах по коммитам после перехода на Issues: там, где проект не завёл собственной компенсации, из 10 закрытых задач страж увидел 2. Там, где проект своим хуком ЗАСТАВЛЯЛ делать закрывающий коммит, — 6 из 8. То есть работоспособность стража держалась не на движке, а на обвязке, которую каждый потребитель изобретал заново.

  **Почему лечится не шаблоном.** `task_close_pattern` верен и компилируется — `bad_regex_issues` здесь бессилен. Дефект не в битой настройке, а в верной настройке, применённой к источнику, который перестал содержать нужный сигнал. Поэтому расширен ИСТОЧНИК, а не шаблон.

### Добавлено
- **`issue_close_watch` — второй источник сигнала о закрытии задачи.** PostToolUse на `Bash` замечает совершённую команду `gh issue close`, кладёт метку в память, а Stop эту метку читает и требует урок тем же порядком, что и коммит-путь. Новая регистрация хука добавляется существующим `install.sh` идемпотентно.

  **Почему PostToolUse, а не PreToolUse.** Нужен ФАКТ закрытия, а не намерение: до выполнения команду может отклонить чужой страж (у обоих живых потребителей на `gh issue close` висит свой привратник основания), может отказать человек в запросе разрешения, может упасть сам `gh`. Метка по намерению — это ложный долг и ложная блокировка завершения. Движок закрытие не запрещает: запрет — политика проекта, дело движка — запомнить и спросить про урок.

  **Почему метка в памяти, а не в TMPDIR.** Прочие метки движка живут в `tempfile.gettempdir()` и теряются при смене TMPDIR между вызовами хуков — известная грабля. Здесь потеря метки означала бы тихий пропуск закрытия, то есть РЕЦИДИВ чинимого дефекта. Метка лежит в `memory_dir` под приватным префиксом (вне git, общий для всех рабочих деревьев, переживает перезапуск). Но `memory_dir` общий и для ПАРАЛЛЕЛЬНЫХ сессий, поэтому в имя метки входит отпечаток рабочего каталога (sha256, не `hash()` — тот рандомизируется per-process): закрытие в одном дереве не блокирует завершение в другом, у которого нет ни контекста задачи, ни повода писать по ней урок.

  **Три предохранителя против вечной блокировки**, и это намеренная избыточность: метка гаснет по найденному уроку с `#<номер>`; по ЛЮБОМУ уроку, записанному позже метки (оба живых проекта именуют задачи слагом, а не номером — точный поиск их не нашёл бы); по сроку годности `task_close_marker_ttl_seconds`. Страж, способный запереть сессию насмерть, хуже отсутствующего: его выключают целиком вместе с пользой.
- Настройки `task_close_command_watch` (дефолт `true`) и `task_close_marker_ttl_seconds` (дефолт 14400 — то же окно, что у `stop_commit_age_limit_seconds`). Мастер-выключателем остаётся `task_close_lesson_gate`. Дефолт ВКЛючён осознанно: у движка уже была история «дефолт, который никогда не работал» (0.11.0), а страж, выключенный по умолчанию, дыру не закрывает — он лишь позволяет сказать, что она закрыта.
- Чек-лист закрытия сессии показывает второй источник отдельной строкой (`task-close-watch`): без неё «task-close включён» читалось бы как «закрытие командой тоже замечается», что неверно при выключенной ручке.

### Изменено
- **Граница коммит-пути названа вслух** в докстринге `stop_check.closure_reminder`: он читает ТОЛЬКО текст последнего коммита и никакого другого способа закрыть задачу не знает. Это было верно и до 0.13.0, но неочевидно — и потребители узнавали об этом на собственных потерянных уроках.
- Хук теперь висит на КАЖДОЙ команде `Bash`, поэтому путь без совпадения сделан бесплатным: ни одного обращения к диску до того, как детектор нашёл закрытие.

### Границы, названные вслух (чинить не планируется)
- Разбор команды оболочки здесь — эвристика, а не грамматика shell: полноценный парсер внутри хука дал бы дефекты хуже закрытых. **Ложный пропуск:** alias'ы и обёртки вокруг `gh`, `xargs gh …`, закрытие через `gh api`/curl, закрытие в вебе или в другом терминале мимо Claude Code — всё это остаётся на попечении коммит-пути. **Ложный детект:** вложенные кавычки и содержимое heredoc, где фраза закрытия лишь упоминается (тестовая обвязка, документация) — цена мала и самогасится любым уроком или сроком годности.
- Обе ЗЕРКАЛЬНЫЕ грабли самого детекта закрыты и закреплены тестами: ложный детект от немаскированных кавычек (`grep "foo; gh issue close" file`) и ложное молчание от поиска признака по всей команде вместо хвоста аргументов самого `close`.
- **Тест на ПРОПУСК обязателен наравне с тестом на блокировку** — и он важнее: мёртвый страж проходит зелёными любые проверки «умеет ли говорить нет». Пропуск засчитывается только при полном молчании (пустой вывод, пустой stderr, нулевой код возврата). Тот же урок, что закрыт в 0.12.0.

### Поймано ревью перед выпуском (все три — один класс: тихая потеря сигнала)
- **Квадратичный разбор хвоста.** Ветка URL в шаблоне номера на длинном СПЛОШНОМ токене со слэшами перебирала слэши с каждой стартовой позиции: замер 40 КБ → 3.9 с, 60 КБ — уже за таймаутом хука (10 с). Это не про скорость: убитый по таймауту хук теряет НАСТОЯЩЕЕ закрытие молча, то есть воспроизводит чинимый дефект новым способом. Хвост ограничен, разбор стал линейным (200 КБ → 3.4 мс), инвариант закреплён тестом на время.
- **Ложный номер вместо неизвестного.** `gh issue close $N --comment "попытка 3"` отдавал задачу «#3» из человеческой фразы. Ложный номер ХУЖЕ неизвестного: он гасит долг по уроку про другую задачу — снова тихий пропуск. Номер теперь ищется только до первого флага; редкая форма `close --repo o/r 5` за это платит потерей номера в тексте, но урок всё равно требуется.
- **Неатомарная запись метки.** `write_text` — это truncate + write, и параллельная сессия успевала прочитать метку пустой, счесть битой и подмести свежее закрытие. Запись переведена на временный файл + `os.replace`. Окно микросекундное, но `memory_dir` общий для всех рабочих деревьев, а параллельные сессии здесь норма.
- Заодно добавлен тест связки «каждое событие из реестра хуков имеет ветку в диспетчере»: опечатка в имени дала бы полностью мёртвый хук, а неизвестное имя диспетчер глотает молча.
- +63 теста (491 против 428).

## [0.12.0] — 2026-07-18

### Исправлено
- **Самодиагностика больше не молчит о разъезде каталогов, когда движок «что-то видит»** ([#5](https://github.com/Arnoldig/claude-memory-engine/issues/5)). Если `memory_dir` не совпадает с каталогом, куда Claude Code пишет авто-память, а `autoMemoryDirectory` явно не задан, `settings_issues()` жаловался ТОЛЬКО при пустом `memory_dir`. Увидев хоть один урок — молчал, хотя `report()` в том же состоянии печатал `same directory: NO`. Добавлена жалоба `self_check.memory_dir_divergent`.

  **Почему прежнее молчание было ошибкой.** Оно держалось на обосновании «возможен намеренно отдельный корпус». Обоснование опровергает сам модуль: уроки создаёт ТОЛЬКО авто-память Claude Code, движок их не пишет никогда. Значит связка «папки разъехались + стражи уроков включены» неудовлетворима по построению — страж требует свежий урок, урок уходит в другую папку, движок не увидит его никогда. Это ровно тот вечный тупик, ради которого проверка и заведена, и попадают в него сами собой: переименование или перенос каталога репозитория меняет слаг, Claude Code начинает писать по соседству, а прежний корпус остаётся мёртвым хвостом — и глушит жалобу собственным существованием.

  **Границы жалобы.** Молчит при выключенных стражах (`stop_lessons_enabled` и `task_close_lesson_gate`) — там отдельный курируемый корпус связен, и движок работает просто ретривером над ним. Молчит при выключенной авто-памяти: «новые уроки пишутся в другую папку» было бы враньём — они не пишутся никуда, и об этом уже говорит отдельная жалоба. Как и прежде, требует подтверждения диском: каталог авто-памяти существует и непуст.

  **Инвариант закреплён тестом:** при включённых стражах И разъезде, ПОДТВЕРЖДЁННОМ диском (каталог авто-памяти существует и непуст), состояние «`report()` печатает `NO`, а `settings_issues()` пусто» стало недостижимым. Оговорка про подтверждение диском существенна: если каталога авто-памяти ещё нет, `report()` по-прежнему печатает `NO` с пометкой «выведено, не подтверждено», а жалоб нет — и это намеренно, иначе движок гадал бы вслух по недокументированному правилу слага.

### Изменено
- **Цена проверки удержана для основного чекаута.** Дешёвым отсекателем git-вызова был glob «вижу ли я уроки», но новая жалоба ровно про случай, когда уроки видны. Заменён на точный и столь же дешёвый: у ОСНОВНОГО чекаута (`.git` — каталог, и в нём есть `HEAD`) путь авто-памяти вычисляется вообще без git, двумя stat (`claude_code_env.default_auto_memory_dir_without_git`), и совпадение с `memory_dir` закрывает вопрос. Контракт закреплён тестом, который падает при удалении отсекателя.

  **Кто всё же начнёт платить:** здоровый проект, открытый из git-worktree или лежащий вне git, — там форму `.git` доказать нечем, и на SessionStart добавляется один git-вызов (~14 мс), которого до 0.12.0 не было. Для основного чекаута — как было. Это осознанный размен: до 0.12.0 «бесплатность» покупалась ровно тем, что проверка не выполнялась.
- `claude_code_env.main_checkout` кэшируется на процесс (`functools.lru_cache`): за одну сессию хука ответ спрашивают несколько путей, а корень чекаута внутри короткоживущего процесса не меняется.

### Разработка (в пакет не входит)
- Появился первый собственный хук репозитория — `.claude/hooks/issue_close_basis_guard.sh`: блокирует `gh issue close` без основания закрытия. Заявка тут закрывается разбором, а не фактом правки; без основания закрытая заявка через месяц неотличима от брошенной.

  Собран из двух живых хуков проектов-потребителей и намеренно не копирует ни один целиком: у одного наличие основания искалось по ВСЕЙ команде (`wc -c f && gh issue close 5` проходил как «основание есть»), у другого не вырезались кавычки (`grep "foo; gh issue close" file` ловился ложно). Взяты обе защиты сразу.

  **Урок дороже самого хука.** Первая версия слегла на синтаксической ошибке bash, а bash отдаёт на ней код возврата 2 — ровно тот, которым PreToolUse ОТКЛОНЯЕТ вызов. Полностью неработающий страж блокировал всё подряд и проходил зелёными все проверки «блокирует ли он что надо». Отсюда правило, закреплённое в `tests/test_issue_close_basis_guard.py`: блок засчитывается только вместе с текстом объяснения, пропуск — только при пустом stderr; одного кода возврата не хватает ни в ту, ни в другую сторону. Это ровно тот же класс, что чинится в этом же выпуске: отсутствие сигнала неотличимо от «всё в порядке».

## [0.11.0] — 2026-07-17

### ⚠ ЛОМАЮЩЕЕ ИЗМЕНЕНИЕ
- **Дефолты `precedent_keyword` / `precedent_pointer` стали английскими**: было `"Прецедент"` / `"перенесён в"`, стало `"Precedent"` / `"moved to"`.

  **Кого касается:** только тех, кто НЕ задавал эти поля в своём конфиге И пишет карточки-прецеденты по-русски. **Что делать:** добавить в `.claude/claude-memory.config.json` две строки —
  ```json
  "precedent_keyword": "Прецедент",
  "precedent_pointer": "перенесён в",
  ```
  Данные не трогаются, уроки не переименовываются.

  **Почему пришлось.** `memory_archive._precedent_re` строится из `re.escape(precedent_keyword)`. Русский дефолт означал, что у КАЖДОГО, кто пишет карточки по-английски и не лез в конфиг, авто-архивация прецедентов не срабатывала НИ РАЗУ, а `precedent_count_warn` не считал НИЧЕГО — молча, при включённом механизме. Это «дефолт, который никогда не работает» — тот самый класс, что `~/.claude/memory` в 0.10.0 (папка, в которую не пишет никто). Правило проекта («generic-дефолт языко-нейтрален, специфика языка — в проектном конфиге») было записано в памяти, но не проверялось — теперь проверяется тестом `test_recogniser_defaults_are_language_neutral`. Русские формы переехали в `examples/claude-memory.config.ru.json`, локализация закреплена отдельным тестом.

  Насколько глубоко это сидело: на русском дефолте молча жили ДЕВЯТЬ собственных тестов движка — то есть английский путь (то, что получает человек из коробки) не проверялся вообще ничем.

### Исправлено
- **Русский пример настроек отстал на шесть релизов.** `examples/claude-memory.config.ru.json` переводил 76 фраз из 133: не переведены были вся самопроверка (17), всё про устаревание (11), уборка архива (6), диагностика (5), жалобы «не понял поле» (3) — то есть ровно то, ради чего делались 0.9.3→0.10.1. Ничего не падало: `msg()` штатно подставляет английский дефолт, поэтому «локализованный пример» молча отдавал вывод на двух языках вперемешку. Английский пример правился восемь раз подряд, русский не двигался с 0.9.2, и внутрь блока `messages` не заглядывал ни один тест. Переведены все 133; закреплено тестом.
- **Мёртвый ключ в каталоге сообщений.** `stale_reconcile.reminder` пережил замену механизма в 0.8.0 и провисел неиспользуемым. Имена ключей — публичный контракт (потребитель переводит их по имени), поэтому мёртвый ключ превращает чужой перевод в молчаливую пустышку: `self_check.message_placeholder_issues` ключи-сироты пропускает по дизайну. Замок `test_every_msg_key_in_code_exists` был ОДНОСТОРОННИМ (код → дефолты); добавлен обратный.
- **Копия правила закрытия в тестах отстала от кода.** `tests/conftest.py::RU_EN_CLOSE_PATTERN` знал шесть слов-закрытий вместо девяти: 0.10.0 научила дефолт семье `resolve`, копию не поправили — и часть сюиты гонялась на правиле, которого в проде нет. Теперь сверяется по ПОВЕДЕНИЮ (копия локализована и обязана отличаться строкой, но не набором распознаваемых форм).
- **Таблица модулей в README не знала о `lesson_files`, `claude_code_env` и `model_registry_guard`** — README тихо врал о составе движка. Поймано владельцем глазами; закрыто тестом «каждый модуль пакета упомянут в README» (оба языка).

### Добавлено
- **CI гоняет тесты на КАЖДЫЙ push и pull request** (`.github/workflows/tests.yml`). Раньше сюита запускалась ТОЛЬКО при выпуске (`publish.yml`, `on: release published`): пул-реквест вливался, не проверив ничего, и поломка всплывала на релизе — когда откатывать дороже всего. Выглядело нормально, потому что не было красных галочек, а не потому что были зелёные.
- **Матрица Python: 3.9 и свежий.** `pyproject.toml` обещает `>=3.9`, а CI гонял `3.x` — то есть обещание совместимости не проверял никто: синтаксис 3.10+ прошёл бы зелёным и сломался бы у пользователя при установке. Инвариант «CI гоняет самую старую заявленную версию» закреплён тестом.
- `tests/test_repo_invariants.py` — новый файл под один класс граблей: значение обязано совпадать в двух местах, никто не проверяет, расхождение молчаливое. Закрывает: версия в `pyproject.toml` vs `__init__.py`; число в бейдже `tests-NNN` vs реальное (за день 0.10.0→0.10.1 бейдж правился руками четырежды и каждый раз устаревал); таблица модулей в README; копия правила закрытия в conftest; матрица Python в CI. Правило, записанное в память проекта: **повторившаяся грабля должна становиться проверкой, а не ещё одним абзацем в заметке.**
- +10 тестов (403 против 393).

## [0.10.1] — 2026-07-17
### Исправлено
- **Подсказка восстановления советовала команду, которой не существует.** Когда путь к урокам не удалось подтвердить, `claude-memory init` печатал «re-run: `claude-memory init --memory-dir <correct path>`». Флага `--memory-dir` нет — путь позиционный, и argparse отвечал `unrecognized arguments`. То есть человеку, у которого путь и так не определился, предлагали команду, которая не запускается: сообщение выглядело помощью, не будучи ею. Тот же класс, что и весь релиз 0.10.0. Теперь печатается исполнимая форма (`claude-memory init <project> <path>`), и это закреплено тестом, который разбирает предложенную команду ТЕМ ЖЕ парсером — правдоподобие не засчитывается (`test_cli.test_derived_path_warning_suggests_a_command_that_actually_runs`, красный на 0.10.0).
- **Нудж о размере/пустом `name` прилетал на файлы в ПОДПАПКАХ памяти — регрессия 0.10.0.** `hooks_cli` звал `is_lesson_file` на голом basename произвольного пути, а имя — лишь половина признака: `lesson_paths` обходит только КОРЕНЬ `memory_dir`. То есть модуль, заведённый как единый источник истины, давал ДВА разных ответа на «что такое урок» — ровно то расхождение, ради искоренения которого он и создан. Особая издёвка: писать в `drafts/<сессия>.md` велит сам движок (`session_marker_guard`), черновик сессии ДЛИННЫЙ по замыслу, а имя у него — случайный uuid, в `size_exempt` не занесёшь; у боевого проекта такая подпапка есть. Введена `lesson_files.is_lesson_path` (корень + имя); у кого на руках путь — зовёт её. Закреплено двумя тестами, включая инвариант «`is_lesson_path` и `lesson_paths` никогда не расходятся».
- **Сюита зависела от домашней папки того, кто её запускает.** Движок с 0.10.0 читает настройки хозяина, а слабейшая их область — `~/.claude/settings.json`. Тесты «при битой настройке молчим» проходили только потому, что у автора там нет `autoMemoryDirectory`; с `HOME`, где он задан (законная настройка!), падало ПЯТЬ тестов — из них ТРИ старых, которых правка не касалась вовсе. Тест, зависящий от чужого ноутбука, закрепляет ноутбук, а не поведение. Общая autouse-фикстура `isolated_home` (conftest) подменяет домашний каталог для КАЖДОГО теста, оба канала (`Path.home` и env `HOME`). Проверено: 390 зелёных на четырёх разных `HOME`, включая выключенную авто-память и полное отсутствие `~/.claude`.
- **Флагманский анти-воскрешающий тест проверял у стража ОДНО ЧИСЛО вместо набора.** Для каталога и ретривера сверялись множества, а для стража — только `max(mtime)`. Любое сужение набора, не задевшее самый свежий файл, проходило зелёным: например, страж, снова теряющий типы `role`/`prompt`/`principle`, оставался незамеченным. Теперь набор доказывается ПОЭЛЕМЕНТНО (каждый файл по очереди делается свежайшим) плюс обратная проверка: ядро/указатель/приватный файл, ставшие свежайшими, страж видеть не должен.
- **Форма пути авто-памяти не была закреплена ничем** — тесты брали ожидаемое значение из самой проверяемой функции. Подмена `projects` на что угодно возвращала бы дефект «движок ищет уроки в несуществующей папке» при зелёной сюите. Форма `~/.claude/projects/<slug>/memory` — центральный факт релиза, и вышестоящего контракта у неё нет (правило слага не задокументировано), поэтому закреплена литералами сегментов.
- **Устаревшие тексты CLI и `install.sh`.** Шапка `install.sh` обещала «creates the memory directory if it does not exist yet» — по выведенному пути каталог намеренно НЕ создаётся (пустышка рядом с настоящей папкой замаскировала бы жалобу самодиагностики). Help команды `doctor` гласил «config self-check (message placeholders)» — проверок пять, включая главную: сверку путей с настройками Claude Code.
- **Справка `install.sh --help` молча обрезалась.** Она собирается из головного комментария, а диапазон был зашит НОМЕРАМИ строк (`sed -n '2,16p'`): любая правка шапки роняла хвост справки, и весь раздел Usage переставал печататься. При этом `--help` продолжал что-то выводить — обрезанная справка неотличима от полной, если не знаешь, что должно быть ниже. Поймано dogfood'ом: правя шапку в этом же релизе, я сдвинула строки и обрезала Usage сама. Теперь печатается весь ведущий блок `#` (признак «строка комментария» не требует синхронизации с длиной шапки), закреплено `tests/test_install_sh_help.py` — тест сверяет вывод со СТРОКАМИ шапки из файла, а не со счётчиком.
- **Файл-пример настроек разошёлся с кодом и возвращал уже починенный баг.** `examples/claude-memory.config.json` нёс СТАРЫЙ `task_close_pattern` — шесть слов-закрытий вместо девяти. Пример копируют себе при подключении, поэтому человек ставил свежий движок 0.10.0, копировал пример и получал ровно ту поломку, которую релиз чинил: `Resolves #12` молча не распознаётся, привратник закрытия не срабатывает. «Синхронить examples с дефолтами» уже значилось граблёй процесса выпуска — известной человеку, но не проверяемой машиной, а потому воспроизводимой ровно тогда, когда релиз спешит. Теперь проверяется машиной: `tests/test_examples_sync.py` сверяет нейтральные дефолты примера с кодом ПО ЗНАЧЕНИЮ и отдельно ПО ПОВЕДЕНИЮ (пример обязан узнавать все девять форм закрытия — иначе осмысленная правка могла бы разойтись, оставаясь «похожей»), плюс ловит в примере неизвестные ключи: опечатка в имени ключа тиражировалась бы всем, кто скопировал.
- **README (оба языка) советовал конфигурацию, которая ЛОМАЕТ движок**: «один и тот же путь к памяти у всех проектов даёт общий пул уроков». Средствами движка общий пул не делается — уроки пишет авто-память Claude Code, а она по умолчанию ведёт память отдельно по проектам; `memory_dir` в общей папке = чтение каталога, в который никто не пишет. (Свести проекты в одну папку может только сам Claude Code — через `autoMemoryDirectory`; это теперь описано.) Плюс быстрый старт обещал, что `init` «создаёт папку для уроков» (он её НАХОДИТ и намеренно НЕ создаёт по выведенному пути); самопроверка была описана как одна проверка из пяти — без той, что сверяет пути с Claude Code; `doctor` назывался командой проверки, хотя картину настройки печатает только `python3 -m claude_memory.self_check`; фраза «движок уроки только читает» была неверна — в существующие файлы он пишет (авто-архивация вырезает старые карточки прецедентов). Бейдж тестов: 200+ → 390.

## [0.10.0] — 2026-07-17
### Исправлено
- **Страж перестал слепнуть на уроках, которые сам же велел записать.** Движок определял урок по началу имени файла (`feedback_`/`reference_`/`project_`), но файлы уроков создаёт НЕ он: их пишет встроенная авто-память Claude Code (её подпись `metadata.originSessionId` стоит в каждом файле; доказано на репозитории самого движка, где библиотека не установлена, а 10 файлов уроков есть). Урок, названный иначе, для каталога и ретривера существовал, а для стража — нет: страж требовал зафиксировать урок, урок записывали, страж требовал снова. Читалось как «хук сломан и ругается всегда». Теперь уроком считается ЛЮБОЙ `*.md` в корне `memory_dir`, кроме ядра, указателя и приватных `_*` — единый источник истины `lesson_files.is_lesson_file`/`lesson_paths`, общий для стража, привратника закрытия, каталога, ретривера, `applies_to`, `staleness` и bloat-check. **Миграция не нужна: ни один урок не требует переименования или переноса.** Расширение строго МЯГЧЕ — оно может только снять блок, но не создать новый. Замер на боевых корпусах: в поле зрения стража возвращается +5 уроков (427) и +13 (87).
- **Тип урока берётся из поля `type`, а не угадывается по имени файла** (`lesson_files.lesson_type`). Модель именует файл по формуле `<значение type>_<слаг>.md` (замер: у 421 файла из 426 приставка ТОЧНО равна полю), но типов в ходу девять, а `lesson_prefixes` знал три. Поле `type` присутствует в 513 файлах из 514 — оно надёжно, имя нет. Фолбэка на приставку намеренно НЕТ: он законсервировал бы то самое второе определение урока. Файл без поля получает пустой тип — честный результат, видимый в пульсе здоровья.
- **`user` добавлен в дефолтные `lesson_prefixes`.** Официальный словарь типов авто-памяти Claude Code — `user | feedback | project | reference`, а дефолт библиотеки знал три из четырёх: урок типа `user` был невидим стражу в ЛЮБОМ проекте на дефолтах, по конструкции (в обоих боевых корпусах — ровно по одному такому).
- **Установщик больше не разводит каталоги по умолчанию.** `claude-memory init` без явно заданного пути прописывал `~/.claude/memory` — каталог, в который не пишет никто, тогда как Claude Code держит память в `~/.claude/projects/<slug>/memory`. То есть разъезд каталогов был ПОВЕДЕНИЕМ ИЗ КОРОБКИ, а не ошибкой пользователя: движок читал пустую папку, страж требовал урок, которого там никогда не появится. Теперь путь ищется у хозяина (`claude_code_env.resolve_auto_memory_dir`): подтверждённый диском — берётся молча, невыведенный — берётся с явной оговоркой и БЕЗ создания каталога (пустышка рядом с настоящей папкой замаскировала бы ошибку от самодиагностики). Из git-worktree корректно находится каталог основного чекаута — память общая на весь репозиторий.
- **Сверка `[[ссылок]]` между уроками больше не пропускает уроки без приставки** (`catalog_generate.find_broken_wikilinks`). Валидировались только цели, начинающиеся с префикса урока: в боевом корпусе ВСЕ связи kebab-уроков не проверялись вообще — оборвись они, никто бы не узнал. Признак ссылки теперь ФОРМА цели (составной слаг, опц. `.md`), а не имя файла. На боевых корпусах вскрылось 10 и 1 настоящих оборванных ссылок.
- **Привратник закрытия задачи узнаёт ВСЕ девять слов-закрытий GitHub.** Дефолтный `task_close_pattern` знал `close/closes/closed` и `fix/fixes/fixed`, но не семью `resolve/resolves/resolved` — то есть треть законных форм закрытия GitHub привратник молча не узнавал, а «не узнал» неотличимо от «задачу не закрывали». Поймано при переводе трекера с файлов `.md` на GitHub Issues. Тот же класс дефекта уже описан в памяти проекта (страж, узнающий событие по НЕПОЛНОМУ списку форм, недосрабатывает молча): список форм обязан зеркалить ИСТОЧНИК — документацию GitHub, — а не интуицию автора. +3 теста, включая параметризованный по всем девяти словам и контроль «голое упоминание `(#5)` — не закрытие».
- **`staleness` и `applies_to` переведены на общий набор уроков.** Они глобили `*.md` вообще без исключения ядра/указателя/приватных — третий вариант определения урока, живший своей жизнью. Сегодня не стреляло (у ядра нет frontmatter), но это ровно та мина, что подорвалась у стража.
### Добавлено
- **Самодиагностика расхождений с настройками хозяина** (`self_check.settings_issues`, SessionStart + CLI). Три жалобы, от смертельной к бытовой: (1) авто-память Claude Code ВЫКЛЮЧЕНА (`autoMemoryEnabled: false` или env `CLAUDE_CODE_DISABLE_AUTO_MEMORY`), а стражи уроков включены → уроки писать НЕКОМУ вовсе (движок их не создаёт, он читатель), Stop блокирует после каждого коммита и удовлетворить его нечем — единственная комбинация с настоящим вечным тупиком, достижимая одной строкой в settings.json; (2) `autoMemoryDirectory` задан явно и ≠ `memory_dir`; (3) `memory_dir` пуст, а в папке авто-памяти уроки есть — наследие сломанного дефолта установщика. Жалоба (3) выдаётся ТОЛЬКО когда догадка подтверждена диском (своих уроков ноль И у хозяина они есть): правило вывода слага не задокументировано, и гадать вслух движок не вправе. Fail-open: нет файла / битый JSON / git недоступен → молчание.
- **Отчёт самопроверки**: `python3 -m claude_memory.self_check` печатает КАРТИНУ всегда, а не только жалобы — путь движка, путь авто-памяти Claude Code, совпадают ли они, сколько уроков видно и каких типов. Жалобы отвечают «что сломано», и при чистом конфиге их нет — человеку было нечем ПРОВЕРИТЬ настройку, только узнать о поломке. А проверять надо: разъезд каталогов выглядит ровно как «всё хорошо, уроков просто пока нет».
- Новый модуль `claude_code_env` — знание о среде-хозяине: правило слага (выведено обратной инженерией, проверено на трёх боевых проектах включая кириллические пути), каталог основного чекаута из worktree, области `settings.json` (local > project > user), `autoMemoryDirectory`, киллсвитчи авто-памяти. Догадка никогда не подаётся как истина: либо подтверждена диском, либо сопровождается оговоркой.
- Поле конфига `precedent_files` — явные файлы-накопители прецедентов для авто-архивации. Контракт «`lesson_prefixes[0]` — это файл прецедентов» был позиционным и нигде не описанным: проект с другим порядком префиксов молча целился не в тот файл (живой случай — первый префикс `urok`). `None` → историческое поведение бит-в-бит.
- +70 тестов, включая анти-воскрешение: `test_lesson_files.test_all_consumers_see_the_same_set` требует, чтобы каталог, ретривер и страж физически видели ОДИН набор. Пока тест зелёный, баг не воскреснет. Плюс сквозной сценарий «свежий коммит → блок → записали kebab-урок → блок снят», которого раньше не было (иначе баг бы не дожил).
### Изменено
- `lesson_prefixes` **УСТАРЕЛО** и больше не отвечает на вопрос «что такое урок» — на него отвечает `lesson_files.is_lesson_file`, а тип берётся из `type`. Поле СОХРАНЕНО (конфиги внешних потребителей не ломаем) в узких ролях: фолбэк для `size_warn_prefixes` и `precedent_files`, ссылки в `precedent_index`.
- `size_warn_prefixes: None` теперь значит «все уроки», а не «уроки с приставкой»: урок без приставки рос без единого предупреждения. Само поле остаётся СУЖАЮЩЕЙ ручкой.
- Граница `self_check` переформулирована ПО ФУНКЦИЯМ, а не по модулю: право бежать на каждом SessionStart принадлежит `warnings()`/`run()` (конфиги + метаданные каталогов, содержимое уроков — никогда), а `report()` живёт только в verbose-CLI по явной просьбе человека и содержимое читает (разбивка по типам ради того и заведена). Модульный инвариант «никогда не читаем уроки» опровергался бы собственным `report()` — и следующий человек «починил» бы противоречие, либо ослабив инвариант до бессмыслицы, либо выпотрошив отчёт.
- `settings_issues` на здоровом проекте ускорен 15.3 мс → 0.7 мс: дешёвый glob «вижу ли я уроки» стоит ПЕРЕД git-вызовом (14 мс), поэтому за диагностику платит только тот, кто уже сломан. Чтение settings.json — 0.15 мс на область, в оптимизации не нуждается.
- Удалён `catalog_generate._skip_basenames` — мёртвая функция и уцелевший обломок второго определения урока: рабочая, вызываемая одной строкой, с виду легитимная. Ровно так болезнь и возвращается — в релизе, чья суть «определение одно», готовая копия старого не имеет права жить.

## [0.9.7] — 2026-07-16
### Добавлено
- Предупреждение о раздутом `description` в момент записи урока (`bloat.description_long`, PostToolUse `bloat-check`, порог `description_warn_chars`, по умолч. 500 знаков, 0 = выкл). `description` — это КРАТКОЕ содержание, и страж правки с ретривером печатают его ЦЕЛИКОМ: несколько раздутых уроков на одном файле дают стену текста, которую перестают читать (тогда привязки бессмысленны). Обрезать при показе — не лечение: теряется контекст, ради которого урок и писали, поэтому чиним источник и в момент, когда автор рядом. Порог — ~2.5× медианы боевых корпусов (202 и 173 знака), то есть задевает не «подробные» уроки, а те, где тело переехало в описание или склеены две темы (на корпусах: 5 из 427 и 12 из 87; живой пример — описание на 1209 знаков, вторая половина которого начинается с «CSS issue #3», то есть в файле два разных урока). Срабатывает только на записи — существующие уроки не нудят. +3 теста.
- Движок ЖАЛУЕТСЯ ВСЛУХ, когда `applies_to:` задан, но глобов из него не вышло (`applies_to.unparsed_applies_to`) — раньше любое непонятое значение молча давало пустоту, неотличимую от «привязок нет» (третий случай этой болезни: ср. `topic` чужой таксономии, тихо выпадавший в «Без темы», и Stop-гейт, молчавший на русскую фразу закрытия). Два канала: (1) НЕМЕДЛЕННО в момент записи урока — PostToolUse `post-record` (`applies_to.unparsed_warning`) называет файл и само непонятое значение, пока автор дефекта рядом; (2) СВОДКА по всей памяти — `staleness.scan_unparsed` → секция в `_stale_pending.md`, которую следующий SessionStart печатает целиком и без троттлинга, пока не починят (соседняя полка к протухшим привязкам). В стража правки жалоба намеренно НЕ ставилась: сломанный урок не привязан к правимому файлу и сыпался бы на каждую правку любого файла. Ключ `applies_to_value` отличает «поля нет» (`None`) от «поле есть, значение пусто» (`""`) — на этом различии стоит жалоба. Тексты переопределяемы через `messages`. +7 тестов (обнаружение всех непонятых форм; обе жалобы; молчание на исправных уроках, на чтении и вне каталога памяти).
### Изменено
- Списочные поля конфига приводятся к каноническому виду в `MemoryConfig.__post_init__`, если описка ОДНОЗНАЧНА по намерению: `retrieve_extensions: [".py"]` → `py`, `watched_dirs: ["./app/"]` → `app`, `lesson_prefixes: ["feedback_"]` → `feedback`, `staleness_skip_dirs: [".git/"]` → `.git`. Здесь движок намеренно НЕ жалуется, а понимает — и это не исключение из правила «не отменять молча явно заданное», а следование ему: настройка начинает РАБОТАТЬ вместо того, чтобы тихо выпадать. Жалоба остаётся там, где намерение неясно (описка в ИМЕНИ ключа → `self_check.typo_key_issues` спрашивает), а `.py` не может означать ничего, кроме `py`. Каждый случай до этого ломался молча и воспроизведён: `.py` → `_path_re` собирал `\.(?:\.py)` (требовал `..py`) → канал «уроки по пути из запроса» не находил ничего; `app/` → там же требовалось `app//`; `feedback_` → `catalog_generate` клеил `feedback__`, `stop_check` искал `feedback__*.md` → движок считал, что уроков нет вообще, и Stop-страж переставал видеть записанное (при этом `startswith` в другом месте работал — поведение расходилось между частями движка); `.git/` → сверка идёт с голым именем каталога от `os.walk` → пропуск не срабатывал и обход лез в тяжёлые каталоги. Нормализация в `__post_init__`, а не в `_coerce`: инвариант обязан держаться при ЛЮБОМ способе создания конфига (JSON / дефолты / `dataclasses.replace`), иначе разойдутся боевой путь и тесты. Элементы, схлопывающиеся в пустоту (`"."`, `"/"`, `"_"`), выбрасываются — пустая ветка в regex совпадала бы с чем угодно. На живых конфигах обоих проектов изменений 0 (там всё каноническое). +4 теста.
- Служебный каталог `.claude/` больше не пропускается стражем правки СКОПОМ — правило стало точным и переехало из `hooks_cli` в `applies_to.find_lessons_for_path` (одно на оба канала): внутри `.claude/` матчат только те глобы, что сами адресуют `.claude/`. Прежний общий пропуск защищал от реального (широкий `*.py` — `fnmatch` `*` матчит и слэши — не должен всплывать на служебных файлах), но заодно молча отменял ЯВНЫЕ привязки к настоящему коду проекта, который там живёт: у одного проекта в `.claude/hooks/` лежат стражи выкладки, у другого есть отдельный урок, ПРЕДПИСЫВАЮЩИЙ привязывать уроки о движке к вендоренной копии (`.claude/memory_engine/claude_memory/…`) — то есть движок глушил собственное задокументированное соглашение проекта. Итого 12 уроков в двух проектах не всплывали никогда. Вдобавок половины движка расходились: `memory_retrieve._path_re` `.claude/` НИКОГДА не пропускал, и по упоминанию пути в запросе урок находился, а на правке того же файла молчал. Теперь обе правды уживаются. Проверено на живых проектах: срабатывает ровно на явно привязанных файлах (8 из 72 и 6 из 33 файлов в `.claude/`), случайных срабатываний от широких глобов — 0; прежний регресс-замок (`test_pre_edit_gate_fires_in_worktree_but_skips_tooling`) зелёный без правок. +3 теста.
### Исправлено
- Поля-даты урока (`reverify_after`, `archived_on`) читались строгим regex `(\d{4}-\d{2}-\d{2})`, который СЛИВАЛ «поля нет» и «поле есть, но не дата». `reverify_after: "01.01.2026"` (естественная русская запись) МОЛЧА не существовал: урок выглядел time-bound и жил вечно; `archived_on` в том же виде навсегда проходил мимо `archive_stale_months`. Жалобы не было никакой — ни в пульсе, ни в `_stale_pending`. Теперь `staleness._date_or_complaint` даёт РОВНО три состояния (поля нет / разобрано / задано мусором), причём детектор и парсер — один код (разъехаться не могут, иначе получили бы либо ложную тишину, либо ложную тревогу). Общие хелперы `applies_to.iso_date_or_none` + `applies_to.field_value` — вместо трёх копий regex+try/except. Доказано 1:1 на боевых корпусах (19 полей с датами: 0 расхождений old↔new, 0 ложных жалоб); +6 тестов.
- Молча выключавшиеся стражи и опечатки в конфиге — теперь их ловит `self_check` (он уже бежит на КАЖДОМ SessionStart и задуман ровно как «поймать настройку, которая тихо портит сеанс», но проверял единственную вещь): (а) `self_check.typo_key_issues` — ОПЕЧАТКА в имени ключа (`session_close_patterns` с лишней `s` → настройка молча выброшена, остаётся английский дефолт; реинкарнация уже случившегося бага со стражем закрытия). Отбрасывание неизвестных ключей сохранено (forward-compat: старый движок × новый конфиг), но популяции разведены по близости к известному имени (`difflib`, stdlib): опечатка по построению близка, честно новый ключ — нет; ключи с ведущим `_` пропускаются всегда (принятое соглашение для заметок в JSON — у живого конфига есть `_task_close_pattern_note`, близкий к `task_close_pattern`, без правила жалоба была бы ложной каждый старт). Текст жалобы сформулирован ВОПРОСОМ («опечатка или ключ новой версии?») — ложное срабатывание не врёт, а спрашивает. (б) `self_check.bad_regex_issues` — битый пользовательский regex (`session_close_pattern`/`task_close_pattern`): движок ловил `re.error` и возвращал «не совпало» → страж молча выключался, а «выключен» неотличимо от «ничего не нашёл». (в) `self_check.bad_date_issues` — `model_registry_verified_on` не в ISO → страж сверки линейки молча выключался, хотя человек его ЗАПОЛНИЛ, то есть намеренно включал. Чтобы это стало возможным, `config._coerce` теперь ПОМНИТ выброшенные ключи в служебном `unknown_config_keys` (из JSON не принимается — иначе конфиг подделал бы отчёт о собственных опечатках): до этого self_check физически не мог увидеть опечатку, конфиг приходил к нему уже очищенным. Ноль ложных срабатываний на обоих живых конфигах; +7 тестов.
- `applies_to` одиночной СТРОКОЙ (`applies_to: "app/x.py"`) — валидный YAML-скаляр, который `applies_to._applies_globs` МОЛЧА ронял в `[]` с рождения модуля (в CHANGELOG отсутствует — не регресс, дыра изначальная). Механика: значение не начинается с `[` → код уходил в ветку разбора YAML-списка, первая же строка ниже (обычно `metadata:`) не начиналась с `- ` → `break` → пустой список. Ни ошибки, ни предупреждения — результат неотличим от «уроков нет», урок никогда не всплывал на правке файла. Разбор ветвится теперь по ЗНАЧЕНИЮ: `[` → инлайн-список; непустой скаляр → один глоб (кавычки снимает общий `strip_scalar`); ПУСТОЕ значение → YAML-список ниже (рабочий формат сохранён — скаляром считается только непустое значение). Значение в фигурных скобках (`{a: b}`, `{app,lib}/x.py`) глобом не считается: ни `fnmatch`, ни `glob` их не раскрывают → отдаётся пусто и срабатывает жалоба ниже с верным диагнозом (иначе такое значение уехало бы в отчёт ПРОТУХШИХ привязок как «путь не найден — файл переехал?», то есть с диагнозом мимо). Доказано 1:1 на боевом корпусе (524 урока с frontmatter в двух проектах, 288 с привязками — 0 расхождений old↔new, 0 ложных жалоб); +5 тестов (скаляр без кавычек/в кавычках/с звёздочкой; граница «пустое значение + список ниже»; контракт `applies_to_value`).

## [0.9.6] — 2026-07-13
### Добавлено
- Версия ЛОГИКИ парсера в отпечатке кэша ретривера (`memory_retrieve._PARSER_LOGIC_VERSION` → `_params_fingerprint`): раньше отпечаток покрывал только ПАРАМЕТРЫ токенизации (стем/мин.токен/окно тела/стоп-слова), но не саму логику разбора (`read_fields`/`tokenize`/`_parse_doc`). Свежесть строки SQLite-кэша проверяется по `mtime_ns`+`size` файла-урока — поэтому не-1:1 правка КОДА парсера при неизменных файлах и параметрах оставляла в кэше стемы, посчитанные СТАРЫМ парсером (тихо неверный поиск; для 0.9.4/0.9.5 приходилось вручную `rm _retrieve_cache.sqlite3*`). Теперь достаточно поднять `_PARSER_LOGIC_VERSION` на +1 при семантической правке разбора → отпечаток меняется → кэш обнуляется сам. +2 теста (смена версии меняет отпечаток; bump реально обнуляет строку кэша, а не отдаёт стемы старого парсера).
### Изменено
- Снятие обрамляющих кавычек у скалярных полей frontmatter вынесено в единый хелпер `applies_to.strip_scalar` и переиспользовано всеми парсерами (`catalog_generate.parse_frontmatter`, `memory_retrieve.read_fields`, `applies_to`, `staleness`) вместо копий идиомы `.strip().strip('"').strip("'")`. Чинит рассинхрон: `applies_to._DESC_RE` и `staleness._DESC_RE` снимали только пробелы → урок с `description: "…"` в кавычках показывался С кавычками в «уроках по пути» и `_stale_pending`, но БЕЗ — в CATALOG и поиске. Только отображение (на ранжирование и состав CATALOG не влияет). Поведение для незакавыченных значений 1:1 (255 прежних тестов зелёные); +3 теста (снятие кавычек в path-lessons/stale/archive + контракт хелпера).

## [0.9.5] — 2026-07-11
### Изменено
- Единый ведущий якорь для `name`/`description` при чтении frontmatter ретривером (`memory_retrieve.read_fields`): раньше поиск читал их с ЛЮБЫМ отступом (`^[ \t]*`), а канонический `parse_frontmatter` и `applies_to`/`staleness` — только top-level (`^`). Рассинхрон: `description` с отступом «видел» бы поиск, но НЕ видел бы указатель CATALOG (половины системы расходились). Приведено к единому top-level (`^name:`/`^description:`), как у остальных парсеров. `keywords` остаются с любым отступом — легитимно живут под `metadata:`. Доказано 1:1 на боевом корпусе (0 расхождений — отступных `name`/`description` в корпусе нет); +1 тест.

## [0.9.4] — 2026-07-11
### Исправлено
- Парсинг frontmatter: у ПУСТОГО значения поля (`name:` или `name: ""` без текста) regex `{key}:\s*(.*)` съедал перенос строки (`\s` матчит `\n`) и захватывал СЛЕДУЮЩУЮ строку frontmatter как значение (пустой `name:` → «topic: …»). Заменено `:\s*` → `:[ \t]*` (только горизонтальный пробел) в 5 парсерах (`catalog_generate.parse_frontmatter` для name/description/topic/subtopic/reverify_after/type, `applies_to._DESC_RE`, `staleness._DESC_RE`, `memory_retrieve.read_fields`). Пустое поле теперь корректно `= ""` (это же чинит незамеченный ранее вариант `no_name` — незакавыченный пустой `name:`). Доказано 1:1 на боевом корпусе (466 уроков двух проектов — 0 расхождений old↔new); +2 теста, падающих на старом коде. Строго-датные regex (`reverify_after`/`archived_on`) и построчный `set_frontmatter_field` не затронуты — безопасны by design.

## [0.9.3] — 2026-07-11
### Добавлено
- Предупреждение о пустом `name` в момент записи урока (`bloat.empty_name`, PostToolUse `bloat-check`): при Write/Edit файла-урока, у которого есть frontmatter, но `name` пуст, хук сразу нуднёт «восстанови заголовок» (name весит ×2 в поиске) — раньше пустой заголовок всплывал только в пульсе следующего SessionStart. Семантика «пустого name» 1:1 с диагностикой `no_name`. Сообщение переопределяемо через `messages`.

## [0.9.2] — 2026-07-02
### Изменено
- Чек-лист итогов памяти перестроен со «снимка до» на «отчёт после» (`stale_reconcile.checklist.header` + `.directive`): вместо просьбы показать блок дословно (данные устаревали сразу после актуализации уроков, а дословный показ моделью не гарантирован — подтверждено живой проверкой: ни `systemMessage`, ни блокирующий `reason` в настольном приложении Claude Code пользователю не видны) ассистенту теперь предписано СНАЧАЛА отработать каждый отмеченный урок, ЗАТЕМ вывести пользователю итоговый отчёт по результату: итог по каждому уроку + статусные строки (сроки/архив, актуальность LLM, стражи) с точными значениями. Только формулировки сообщений (переопределяемы через `messages`); логика и ключи чек-листа не изменились.

## [0.9.1] — 2026-06-30
### Изменено
- Заголовок чек-листа итогов памяти (`stale_reconcile.checklist.header`) усилен: теперь явно просит ассистента ВЫВЕСТИ пользователю весь блок ДОСЛОВНО (все строки и уроки), а не пересказывать. Раньше ассистент мог отработать кандидатов и пересказать итог, не показав сам список пользователю. Только формулировка сообщения (переопределяема через `messages`, т.е. переводима на любой язык); логика чек-листа не изменилась.

## [0.9.0] — 2026-06-30
### Добавлено
- Страж актуальности линейки моделей (`llm_actuality`, новый модуль) — единый страж вместо двух прежних модельных проверок. Совмещает: (1) реактивно на `SessionStart` — если модель сессии не из подтверждённых семейств, сразу нудж «незнакомая модель»; (2) суточно — раз в `llm_actuality_interval_hours` (по умолч. 24) просит ассистента сверить линейку моделей (делегировав дешёвой модели + веб-поиск) и записать итог. Движок офлайн и сам в сеть не ходит. Итог пишется командами `cme_hook.sh llm-verified` (без изменений) / `llm-changes "<что изменилось>" --families <a,b,c>` в приватный файл состояния `_llm_registry_state.json` (он троттлит «раз в сутки» между сессиями и хранит подтверждённый список семейств — сид из `known_model_substrs`). Статус сверки выводится строкой в чек-листе закрытия («LLM actuality: verified <дата>» / «⚠ changes …» / «not verified yet»).
- Новые поля конфига: `llm_actuality_enabled` (по умолч. `true`) и `llm_actuality_interval_hours` (по умолч. `24`).
### Изменено
- На `SessionStart` подстраховку моделей теперь делает `llm_actuality` (реактивная проверка + суточная просьба) вместо прежнего таймера `model_registry_guard` по дате `model_registry_verified_on`. Модуль `model_registry_guard` сохранён (его `resolve_model`/`_is_known` и сообщение `model_registry.unknown_model` переиспользует `llm_actuality`); поля `model_registry_verified_on` / `model_registry_max_age_days` оставлены для обратной совместимости, но в дефолтном пути больше не используются.

## [0.8.0] — 2026-06-30
### Добавлено
- Страж устаревших уроков (`stale_reconcile`) теперь срабатывает на ФРАЗУ ЗАКРЫТИЯ СЕССИИ (хук `UserPromptSubmit`), а не на закрывающий коммит. Когда сообщение пользователя совпадает с новым `session_close_pattern`, в контекст подаётся ЧЕК-ЛИСТ итогов памяти: счётчики (показано уроков при правках / актуализировано / осталось), точные кандидаты на пере-проверку (правил файл с привязанным уроком, но урок не обновил), смысловой список связанных уроков (теперь считается ВСЕГДА при наличии правленых файлов — даже без `applies_to`-привязок), статус сроков годности/архива и список включённых/выключенных стражей. Чек-лист показывается ВСЕГДА при фразе закрытия, даже когда чисто (строка «протухших уроков нет»). Триггер не зависит от коммита/PR/скилла → работает и у проектов без команды закрытия. `/compact` не меняет `session_id` (метки видны), `/clear` страхуется бэкстопом `SessionEnd`.
- Новые поля конфига: `session_close_pattern` (regex фразы закрытия; пусто → чек-лист по фразе выключен) и `session_close_case_sensitive` (учитывать ли регистр; `true` убирает ложные срабатывания на частых словах вроде «Done»).
### Изменено
- Стражи включены «из коробки» (касается ВСЕХ потребителей): `stale_reconcile_gate` `False → True`, `archive_stale_months` `0 → 6`, `lesson_count_warn` `0 → 500` (подсказка про дубли на больших коллекциях) и `known_model_substrs` `() → (opus, sonnet, haiku, fable)` (реактивная проверка «незнакомая модель»; периодическая сверка линейки — отдельно, через `model_registry_verified_on`, по умолчанию выкл). Чек-лист всегда честно показывает, что включено и что найдено, поэтому включение не создаёт ложного впечатления работы. Без `applies_to`-привязок точный список пуст, но смысловой список и сводка стражей всё равно полезны. Прежнее поведение возвращается установкой этих полей в `false`/`0`/`()`.
### Удалено
- Прежний триггер `reconcile_reminder` (разовый блок на закрытии задачи в событии `Stop`) убран — его заменил чек-лист на фразу закрытия. Отдельный привратник «коммит закрыл задачу, а урока про неё нет» (`closure_reminder` / `task_close_lesson_gate`) НЕ затронут.

## [0.7.5] — 2026-06-29
### Изменено
- Упаковка: метаданные лицензии в `pyproject.toml` переведены на формат PEP 639. Поле `license` теперь SPDX-выражение (`license = "Apache-2.0"` вместо TOML-таблицы `{ text = ... }`); классификатор `License :: OSI Approved :: Apache Software License` убран (по PEP 639 лицензия задаётся только полем `license`, дублирующий классификатор устарел); нижняя граница сборочной зависимости поднята до `setuptools>=77.0.0` — версии, где появилась поддержка PEP 639. Это убирает два предупреждения `SetuptoolsDeprecationWarning` на шаге сборки (`project.license` как TOML-таблица и license-классификаторы устаревали и переставали бы работать к 2027-02-18). Поведение установки пакета не меняется; метаданные лицензии на PyPI теперь в актуальном SPDX-формате.

## [0.7.4] — 2026-06-29
### Изменено
- CI: в `.github/workflows/publish.yml` экшены подняты до мажоров, нативно работающих на Node 24, — `actions/checkout@v4 → @v7` и `actions/setup-python@v5 → @v6`. GitHub объявил Node 20 на раннерах устаревшим и принудительно запускал эти шаги на Node 24, выводя предупреждение при публикации; обновление убирает предупреждение. На текущую дату это актуальные стабильные мажоры (checkout v7 от 2026-06-18, setup-python v6.x; оба `runs.using: node24`). Для тривиального использования (checkout без входных параметров; setup-python только с `python-version: "3.x"`) переход — drop-in, поведение не меняется (кэш pip в setup-python v6 остаётся opt-in). Содержимое пакета не изменилось — релиз только прогоняет обновлённый workflow и подтверждает, что предупреждение ушло.

## [0.7.3] — 2026-06-29
### Исправлено
- В дефолтном `task_close_pattern` граница слева заменена с `\b` на негативный lookbehind `(?<![\w-])`. `\b` срабатывает и ПОСЛЕ дефиса, поэтому сообщение коммита вида `prefixed-closes #10` (или `auto-closes #10`) ложно распознавалось как закрытие задачи #10 — `extract_closed_task` возвращал `10` вместо `None`, и привратники закрытия (`closure_reminder`, `stale_reconcile`) могли показать лишнее одноразовое напоминание. Lookbehind по-прежнему пропускает обычные `Closes #id` / `Fixes #id` (в начале строки, после пробела или двоеточия), но не `<слово>-closes #id`. Найдено red-team-проверкой при добавлении русской формы закрытия. Затрагивает только generic-дефолт; потребителям, чей собственный `task_close_pattern` использует `\b` как левую границу перед словом закрытия (в т.ч. если прежний дефолт был скопирован в конфиг), стоит применить ту же замену `\b` → `(?<![\w-])`.

## [0.7.2] — 2026-06-28
### Исправлено
- `extract_closed_task` теперь возвращает ПЕРВУЮ непустую capture-группу совпадения, а не жёстко группу 1, — это позволяет проектному шаблону `task_close_pattern` распознавать закрытие в РАЗНЫХ формах записи, где id стоит по разные стороны ключевого слова. Англ. «Closes #id» кладёт id ПОСЛЕ слова, а локализованная форма вроде русской «#id закрыт» — ДО, и одной capture-группой обе не покрыть. Библиотечный дефолт остаётся английским (generic) — нужную форму проект задаёт в своём `task_close_pattern`. Заодно шаблон без групп больше не падает (None вместо IndexError). Найдено на реальном пропуске закрытия задачи русской фразой «#id закрыт» (без `Closes`): и `closure_reminder`, и `stale_reconcile` молча не сработали. Второй фикс распознавания закрытия после `%B`-фикса v0.7.1 (тело коммита).

## [0.7.1] — 2026-06-28
### Исправлено
- Привратники закрытия задачи (`closure_reminder` и страж устаревших уроков `stale_reconcile`) теперь распознают `Closes #N` и в ТЕЛЕ коммита, а не только в теме (`%B` вместо `%s` в `last_commit_msg`). Раньше при `Closes #N` в теле коммита (как делает GitHub) оба гейта молча НЕ срабатывали. Найдено dogfood'ом на реальном закрытии задачи.

## [0.7.0] — 2026-06-28
### Добавлено
- Страж устаревших уроков на закрытии задачи (`stale_reconcile`, opt-in `stale_reconcile_gate`, по умолчанию выкл). При коммите-закрытии (`Closes #N`) Stop ОДИН раз показывает уроки, привязанные к файлам, которые правились в этой сессии, но сами не были актуализированы, — с вопросом «не устарели ли?» (поправить / пометить неактуальным / заменить новым). К точному списку добавляется смысловой (офлайн-поиск по правленым файлам плюс теме коммита) — ловит связанные уроки даже без привязки по пути. На повторном Stop проходит (разовый блок). Те же кандидаты пишутся секцией в `_stale_pending.md` как бэкстоп на SessionEnd. Fail-open: любая ошибка или невозможность записать метку разовости → не блокировать.
### Изменено
- Метка applies-gate (показ уроков по пути перед правкой) теперь хранит имена показанных уроков — их собирает страж устаревших уроков на закрытии задачи.

## [0.6.0] — 2026-06-21
### Добавлено
- `claude-memory uninstall` — убирает движок из проекта (регистрации хуков, обёртку, конфиг и вшитую копию при установке через git); ваши уроки не затрагиваются.
- Английская версия `README.en.md` рядом с основным `README.md`.
### Изменено
- README переписан для широкой аудитории (от общего к частному; текст отделён от команд).
- Вывод `install.sh` и команд переведён на английский.
### Исправлено
- `install`/`uninstall` больше не падают на «валидном, но не-объектном» `settings.json` или конфиге.
- Версия пакета синхронизирована (`pyproject.toml` отставал от кода).

## Ранние выпуски (2026-06-17 – 2026-06-18)
- **0.5.2** — срок хранения архивных уроков + `archive_prune`.
- **0.5.1** — диагностика: сигнал об уроках с пустым именем.
- **0.5.0** — pip-пакет + установка одной командой `claude-memory init` (плюс `doctor`, `config`, `--version`).
- **0.4.0** — SQLite-кэш для поиска (тот же порядок выдачи, быстрее на больших наборах).
- **0.3.0** — самопроверка конфига и «пульс здоровья» (битые перекрёстные ссылки, подсказка о дублях).
- **0.2.0** — мультиязычные сообщения (английские по умолчанию + переопределение в проекте), полностью настраиваемое поведение, поддержка worktree.
- **0.1.0** — первый движок: уроки с frontmatter, авто-оглавление, офлайн-поиск, уроки по пути файла, авто-обслуживание, страховки для параллельных сессий и суб-агентов.
