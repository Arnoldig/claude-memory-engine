# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.venv/
venv/

# OS
.DS_Store

# Локальный конфиг конкретной машины (НЕ коммитить — у каждой установки свой).
# В репозитории — только examples/claude-memory.config.json (шаблон).
claude-memory.config.json

# ВАЖНО: сами уроки памяти и хранилище памяти в этот репозиторий НЕ входят.
# Здесь живёт только движок. Уроки конкретного проекта — приватны и хранятся отдельно.
memory/
